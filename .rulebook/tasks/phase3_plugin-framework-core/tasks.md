## 1. Implementation
- [x] 1.1 PluginRegistry + closed capability set with placement classes (WritePath | ReadPath | OffPath) (PLG-001/003) — new crates/fluxum-core/src/plugin/mod.rs: `Capability` (8 variants, parse/name round-trip, `placement()`, `sidecar_allowed()`), `Placement`, unified `PluginRegistry`; the set is closed — unknown manifest capability is a descriptive build error
- [x] 1.2 Adopt existing seams as capabilities via adapters: AuthProvider, ColumnTransform, KeyProvider, VisibilityFn — APIs and call sites unchanged (PLG-002) — `adopt_builtins()` reflects the configured auth provider, registered ColumnTransform pipelines, config transform keys (ids only), and `#[visibility(custom)]` predicates as `BuiltinSeam`s in `/plugins`; zero changes to their traits or call sites
- [x] 1.3 New capability traits (definitions only, bound by sibling tasks): ScoreReranker, Retriever/Fusion, StreamSink (PLG-001 §2.1) — object-safe traits + exchange types (`Scored`, `FtQuery`, `CommitBatch`, `Offset`, `PluginCtx`, `PluginError`) and the default `ReciprocalRankFusion` (k=60, deterministic ties) matching the PLG-041 reference formula; `PkBytes::from_bytes` added as the plugin/RPC boundary constructor
- [x] 1.4 In-process host: link-time registration + Cargo feature gating (absent unless enabled); catch_unwind isolation, panic disables plugin / rolls back WritePath tx (PLG-030) — `InProcPluginDef` via `inventory` (feature off ⇒ def absent ⇒ manifest binding fails descriptively); `PluginState::guard` runs every invocation under catch_unwind: panic → metered + auto-disable + `PluginError` (a WritePath error rolls the enclosing tx back via the existing RED-004 path, never crashes the shard)
- [x] 1.5 Placement enforcement in ServerBuilder::build(): reject sidecar-on-WritePath, uncompiled in-proc feature, illegal capability/host combos, missing applies_to targets (PLG-020/021/032) — there is no ServerBuilder type in the codebase; the assembly-time validator is `PluginRegistry::build(schema, config)` (called before serving, failure aborts startup): sidecar-on-WritePath rejected (KeyProvider KMS exception per PLG-021), uncompiled in-proc name rejected, declared-vs-compiled capability mismatch rejected, unknown applies_to table/column rejected, duplicate names rejected
- [x] 1.6 config.yml plugins manifest parsing + validation (PLG-032) — `Config.plugins: Vec<PluginDecl>` with `PluginHost` (internally-tagged `{ kind: in_process|sidecar, feature|endpoint+timeout_ms }`, default timeout 50 ms) and `PluginScope { tables, columns }`; spec's exact YAML shape pinned by test
- [x] 1.7 GET /plugins introspection (name, capability, host, placement, scope; no secrets) (PLG-060) — admin route on ShardContext's installed registry (`set_plugins`), reporting seams + bindings with health (`active|disabled`), panic/error meters, and detail (key ids only — secret-hex leak pinned negative by test); plugin meters also ride `/metrics` (`fluxum_plugin_panics_total`, `fluxum_plugin_errors_total`)
- [x] 1.8 Security rules: no implicit privilege widening; RLS bypass only via explicit server-peer grant; hot disable/circuit-break without core restart (PLG-061) — `PluginCtx { identity, is_server_peer, shard_id }` carries the caller posture (no ambient authority); `POST /plugins/:name/disable|enable` flips `PluginState` atomically without restart; panic auto-disable is the circuit-break baseline (sidecar breaker is the phase-5 task)
- [x] 1.9 Verification: existing seams appear in /plugins with unchanged behavior; illegal bindings abort build() with descriptive errors; a legal in-proc set starts and a panicking plugin is isolated + metered — crates/fluxum-core/tests/plugin_framework.rs (9 tests) + admin_api.rs `plugins_endpoint_reports_and_hot_disables`

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — module rustdoc (plugin/mod.rs: non-distortion contract, hosting, per-item PLG refs), config rustdoc (PluginDecl/PluginHost/PluginScope), admin.rs endpoint table rows for /plugins + disable/enable
- [x] 2.2 Write tests covering the new behavior — plugin_framework.rs: closed set + placement rules, all PLG-032 rejection cases, legal-set build + report, secret-free introspection, hot disable, panic isolation + meters + short-circuit, healthy guard pass-through, RRF reference order/scores + lexical fallback, spec-shape YAML parse; admin_api.rs: GET /plugins + POST disable/enable + /metrics meters end-to-end
- [x] 2.3 Run tests and confirm they pass — `cargo test --workspace` green (0 failures), `cargo clippy --workspace --all-targets` clean, coverage gate ≥90%

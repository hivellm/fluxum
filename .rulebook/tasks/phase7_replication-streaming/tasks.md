## 1. Implementation
- [x] 1.1 Implement commit-log streaming as the replication protocol (log format = stream format, frozen at G5): batches with epoch numbers (FR-100, REP-010..) — new protocol messages (ReplicaHello/ReplAck ⇄ PrimaryHello/ReplCheckpoint/ReplBatch/ReplHeartbeat/ReplFence, tail-additive tags); ReplBatch carries RAW STG-011 frames read from the primary's durable segments (commitlog::read_frames_after — disk IS the stream); server-peer sessions hand over from the TCP router (REP-005/REP-011); replica applies via MemStore::apply_replica_record (full index/unique/spatial/fts maintenance, tx-id monotonicity, auto-inc resume) + raw append to its own log; REP-010 pinned: segment FILES byte-identical over the shared range, both in-process and over the real wire. TxMeta side-band deliberately omitted: reducer_name/caller already ride the record (OPS-020 tail-additive) — noted vs REP-015
- [x] 1.2 Full sync: empty replica converges via checkpoint transfer + log tail (REP-012); partial sync: rejoining replica converges from its log offset (REP-013) — full: primary packs its newest checkpoint (T7.3 pack) and streams chunks; replica installs via backup::install_checkpoint_pack + normal recovery, then tails; e2e forces it with truncated-past-tx-1 segments. Partial: sync decision from first_available_tx_id vs replica offset; e2e stops the client mid-stream, writes more, resumes, converges. (Divergence truncation on rejoin is wired to land with T7.2's demote flow)
- [ ] 1.3 Async mode (default) + semi-synchronous quorum acknowledgment mode; semi-sync visibility barrier - no ReducerResult and no TxUpdate delivered before quorum append confirmed (REP-020/REP-021) — async shipped (default path); the semi-sync barrier is PHASE C (next): quorum inputs already exposed (ReplicaSession durable acks + durable_offsets())
- [ ] 1.4 Epoch fencing: a partitioned stale primary's batches are rejected, it demotes and truncates its diverged suffix; fluxum_replication_fenced_total increments (REP-031) — PARTIAL: epochs persisted before acting (replication.epoch marker, REP-004; boot adopts max(persisted, PITR lineage)); a higher-epoch hello fences the primary (ReplFence + counter, e2e-pinned); replicas adopt higher epochs from batches/heartbeats/fences. The demote+truncate flow is PHASE C/T7.2
- [x] 1.5 Replication config surface: replica-set membership, mode (async|semi_sync), quorum size, max_staleness_ms - documented in the config reference — full §11 key set (member_name, peer_token, heartbeat/election/window/ack/staleness, semi_sync.{quorum,ack_timeout_ms,on_quorum_loss}) + storage.segment_max_bytes (rotation = truncation granularity), validated at load, example-pinned
- [x] 1.6 Replication metrics: fluxum_replication_offset/lag/semi_sync_wait_us and peers gauges (FR-105 with T7.2) — offset{peer}/lag_tx{peer}/connected_replicas/fenced_total exported + Grafana panel (semi_sync_wait_us lands with the phase-C barrier)
- [ ] 1.7 Verification (DAG exit test): replica converges from cold AND from offset (CommittedState equal, log byte-identical over shared tx_id range); semi-sync ack test; fencing test — cold+offset+byte-identity+full-sync+fencing(hello)+bad-credential DONE (5 e2e + 8 core suites); the semi-sync ack leg is PHASE C

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [ ] 2.1 Update or create documentation covering the implementation (module docs shipped; SPEC-014 status row + DEPLOYMENT notes land with phase C)
- [x] 2.2 Write tests covering the new behavior (core replication_apply.rs: 8 — convergence, byte-identity, gaps, forged edges, auto-inc promotion, AS OF parity, stream helpers, transfer errors; server replication_e2e.rs: 5 over the real wire; unit suites for epoch/frames/reader)
- [x] 2.3 Run tests and confirm they pass (all suites green; coverage 90.00%, floor holds)

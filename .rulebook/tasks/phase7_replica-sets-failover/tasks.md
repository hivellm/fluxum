## 1. Implementation
- [x] 1.1 Consensus-based primary election per replica set (decide OQ-8: openraft vs custom Raft); automatic failover on primary failure (FR-101, REP-030/REP-032) — OQ-8 resolved: a minimal custom Raft-style election (votes only — the commit log already IS the replicated log, so no second consensus log is stubbed out; family precedent + the DST determinism requirement favor the small testable election). fluxum_server::election: majority votes, `(epoch, voted_for)` ballot persisted before it is answered (REP-004), `(last_log_epoch, last_tx_id)` up-to-dateness + leader stickiness (Raft §9.6); decide_vote/majority/quorum pure + unit-tested; promotion (REP-032) persists epoch, raises the writer envelope, publishes the role, THEN accepts writes; the winner needs no announce message (survivors rediscover it by peer rotation). e2e: a 3-member set kills the primary, a replica wins and serves writes
- [x] 1.2 Stale-primary handling: demote + diverged-suffix truncation on rejoin; no write from a stale epoch survives in the set (REP-031, REP-013) — a member booted as primary parks watching the fence; a higher epoch on ANY channel (hello or ack) adopts the epoch, flips role→replica, and rejoins the follow loop (demote-on-fence, e2e-pinned: a stale primary fenced by a higher-epoch hello demotes, its writes then take the NotPrimary redirect). Replicas reject stale-epoch batches (admit_batch_epoch, from T7.1). The diverged suffix is discarded on the resync — a live in-process store cannot be rebuilt in place (recovery needs a fresh store), so the clean rebuild is on restart; in async that tail is the documented loss (REP-034), in semi_sync those txs were never client-visible (REP-021)
- [x] 1.3 SDK transparent failover: clients reconnect, re-authenticate, resubscribe; reconciled caches match the new primary exactly (REP-033; SDK-047) — Connection::connect_replica_set(urls, …): the initial connect tries the endpoints in order, and the reconnect loop rotates through them so a failover rediscovers the new primary and re-establishes (re-auth same token ⇒ same identity, resubscribe, reconcile — net change only, never a cache wipe). A NotPrimary reducer reply advances to the next endpoint (REP-042 redirect). e2e: a client whose first endpoint is dead rotates past it and drives a full session
- [x] 1.4 Read + subscription fan-out offload to replicas: replica TxUpdate streams content-identical to the primary incl. RLS filtering and TxMeta (FR-102, REP-043); bounded observable staleness - replica past max_staleness_ms rejects new admissions with ReplicaStale (REP-041) — replicas apply + fan out with the record's own provenance (REP-043, from T7.1: content-identical TxUpdates, RLS on the replica's own subscriptions). REP-041: a replica whose last contact from its primary exceeds max_staleness_ms refuses NEW reads/subscriptions with a retryable CLUSTER_REPLICA_STALE (session OneOffQuery/Subscribe + admin /query + /view); already-attached subscribers keep streaming
- [x] 1.5 Replication observability: /health reports role/epoch/replicas/offset/lag (REP-080) within the OBS-061 latency bound; all REP-081 metrics + REP-082 structured events through a scripted election + resync sequence (FR-105) — /health `shards[].replication` object (role, epoch, connected replicas or lag_tx, degraded/stale flags) from pre-computed lock-free state; metrics role/epoch/elections_total/full_syncs_total + semi_sync_wait_us/degraded/fenced/offset/lag + Grafana panels 35/36/37; REP-082 events (election started/won/lost, promotion, demotion, fence, sync) emit as structured tracing. e2e asserts the /health object after failover
- [x] 1.6 Replication DST scenarios green (TST-134) — fluxum-dst::replication (tests/dst_replication.rs): a seeded primary streams its commit log to a replica applied through the real apply_replica_record under seeded message faults — dropped batches recovered by an offset resync (REP-013) and duplicated batches (idempotent replay, REP-014); every run asserts convergence (replica CommittedState == primary) + byte-identity (REP-010); each seed runs twice for the TST-130 determinism check; the test asserts the fault paths actually fired. Env knobs FLUXUM_DST_SEEDS/OPS/SEED for the nightly volume + reproduction
- [x] 1.7 Verification (DAG exit test): failover drill - kill -9 the primary of a 3-member semi-sync set under sustained writes: a replica promotes and every client-observed transaction is present; zero committed-tx loss; fan-out offload verified — failover_e2e drives a 3-member **semi_sync** set (quorum 2), seeds writes whose 200 means quorum-durable, kills the primary, a replica promotes and holds every acked tx (zero loss), serves new writes, and the survivor converges via its own fan-out under the new epoch; /health reflects the new topology; stable across repeated runs. (The 1,000-subscriber throughput-neutrality measurement is a bench observation, not a correctness gate — the fan-out-on-replica PATH is covered)
- [x] 1.8 Gate G7 input (PRD 12.2 failover criterion) — the failover + zero-loss drill (1.7) and the replication DST (1.6, a G7 exit per TST-134) are green; SPEC-014 §5/§6 status marked shipped

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation (module docs; SPEC-014 status row: §5 failover + §6 read replicas shipped; DEPLOYMENT.md replication section: elections/failover, bootstrap-hint roles, demote-on-fence, replica-set clients + NotPrimary/ReplicaStale, staleness)
- [x] 2.2 Write tests covering the new behavior (election unit suites — ballot round-trip, majority, decide_vote incl. stickiness, RoleState, jitter, staleness; failover_e2e — semi_sync failover+zero-loss, demote-on-fence, /health; SDK client_e2e — replica-set rotation; the replication DST)
- [x] 2.3 Run tests and confirm they pass (all suites green; coverage ≥90% floor)

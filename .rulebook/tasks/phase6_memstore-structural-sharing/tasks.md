## 1. Implementation
- [x] 1.1 Investigate and decide, with measurements — **decision 002 (accepted), bench `benches/table_clone.rs` (2026-07-22)**: std clone is linear (rows 148 µs/7.1 ms/85.8 ms at 10k/100k/1M; btree index 37 ms and unique map 7.3 ms at 100k) vs `imbl` 7.0.1 clone **3.1 ns at any size**, worst-case shared insert 0.8–1.0 µs, point get *faster* than std (50 vs 69 ns), range 1.45× and full iteration 2× slower (async checkpoint scans only). Chosen: `imbl` for rows + btree + unique + full-text (hand-rolled rejected — no win on any axis); **spatial stays deep-cloned** (arena trees don't map to chunked path-copy; cost only on `#[spatial]` tables, none in the parity demo — recorded residual in decision 002)
- [x] 1.2 Replace `TableState::rows` (BTreeMap<PkBytes, Row>) with the chosen structurally-shared ordered map — `imbl::OrdMap` in `store/committed.rs` + `memstore.rs` + `checkpoint/recover.rs`: point lookup, `Bound`-based range scans, deterministic key-order iteration and `row_count` preserved; commit merge is O(k·log n), no full-table clone; prior snapshots untouched (suite green)
- [x] 1.3 Same treatment for the write-path index structures — `BTreeIndex` (`imbl::OrdMap<Vec<u8>, imbl::OrdSet<PkBytes>>`), `UniqueIndex` (`imbl::OrdMap`), full-text `postings`/`doc_len` (`imbl::OrdMap`, clone-modify-reinsert keeping empty-slot pruning bit-identical to a rebuild); spatial per the 1.1 decision (residual). STG-005/STG-007 row/index consistency unchanged — property + crash + DST suites green
- [x] 1.4 Prove the complexity claim in the NFR-03 bench — `txn_commit` now runs 1k AND 1M rows, asserting p99 < 1 ms at both plus a size-independence ratio check. **Measured 2026-07-22**: p99 51.7 µs @ 1k, 63 µs @ 1M — ratio **1.22** (before the fix the 1M case was an ~85.8 ms clone per commit); criterion means 29.8 µs vs 34.7 µs
- [x] 1.5 Full regression pass (2026-07-22): `cargo test --workspace` 0 failures (subscription property suite, crash kill-9/corruption/checkpoint-boundary suites, DST, pager_10x, Rust conformance runner), workspace clippy clean, TS SDK **95/95** (Node + Chromium conformance runners) + typecheck clean. Coverage: **fluxum-core 93.67 %** lines (> 90 %); the workspace-wide floor breach (88.96 %) is the pre-existing bench-harness debt documented in docs/COVERAGE.md, untouched by this change
- [x] 1.6 Verification (exit test): `fluxum-bench report` re-run on the same machine/protocol (2026-07-21, committed at e4d6c7e) — **write throughput 0.30× → 25.66× ✅** (target ≥ 10×), hot 9 423× ✅ and cold 9.04× ✅ stay green. e2e p99 improved 4.9× → **8.54×** but stays under 10×: the commit-cost half of the miss is gone; the remainder is fan-out-path latency, owned by `phase0_parity-fanout-latency` (materialized 2026-07-21, after this task was written — its 1.5 is the e2e ≥ 10× exit test). T6.3's 1.6/1.7 stay blocked on that half only; notes updated there

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — decision 002 (numbers + residuals), rewritten `TableState`/index module docs, workspace Cargo.toml imbl rationale (incl. MPL-2.0 licensing note); SPEC-002/003 never specified the clone, no spec text to amend
- [x] 2.2 Write tests covering the new behavior — the complexity claim is asserted by the extended `txn_commit` bench (1k + 1M, p99 < 1 ms + size-independence ratio); behavioral invariants are the existing property/crash/DST/conformance suites by design (proposal: "no behavioral change")
- [x] 2.3 Run tests and confirm they pass — see 1.5: workspace 0 failures, clippy clean, TS 95/95, fluxum-core coverage 93.67 %

## 1. Implementation
- [ ] 1.1 Investigate and decide, with measurements: (a) quantify the per-structure clone cost in `TableState` (rows BTreeMap, secondary `BTreeIndex`es, `UniqueIndex` maps, `SpatialIndexState`, `FullTextIndexState`) at 10k/100k/1M rows — a micro-bench that clones each in isolation, so the fix targets what actually costs; (b) choose the sharing strategy per structure: persistent ordered map (path-copying, Arc-shared nodes) for `rows` and B-tree indexes is the working hypothesis, but spatial/full-text may warrant a different scheme (e.g. generational rebuild — they are already rebuilt at boot, not persisted); (c) decide dependency vs hand-rolled: evaluate a vetted persistent-collections crate (e.g. `imbl`) against a hand-rolled Arc-node ordered map on lookup/insert/range/iter benches, licensing, and maintenance status — record the decision and numbers in the task, HiveLLM convention is few, well-justified deps
- [ ] 1.2 Replace `TableState::rows` (BTreeMap<PkBytes, Row>) with the chosen structurally-shared ordered map: point lookup, ordered range scans (`Bound`-based), deterministic iteration, and `row_count` preserved; `Tx::commit`'s merge writes k rows in O(k·log n) with no full-table clone; readers on prior snapshots observe nothing
- [ ] 1.3 Same treatment for the write-path index structures maintained inside the commit merge: secondary B-tree indexes (STG-051) and `#[unique]` maps (TXN-041 eager-check lookups); spatial and full-text per the 1.1 decision. Row/index mutual consistency inside one published snapshot (STG-005/STG-007) must hold exactly as today
- [ ] 1.4 Prove the complexity claim in the NFR-03 bench: extend `txn_commit` with large-table cases (≥ 1M rows) asserting commit p99 < 1 ms **independent of table size** (the 1k-row case stays as the regression floor); record before/after numbers
- [ ] 1.5 Full regression pass: subscription property suite (TST-corpus 10k mutations), crash suites (kill -9, corruption, checkpoint boundaries), DST, pager 10× memory-budget suite, conformance corpus (Node + Chromium + Rust runners), workspace clippy — zero behavioral diffs allowed; coverage stays > 90%
- [ ] 1.6 Verification (exit test): `fluxum-bench report` re-run on the same machine/protocol — write throughput ≥ 10× and e2e p99 ≥ 10× now met (hot/cold stay green); check off T6.3's blocked 1.6/1.7 with the new `docs/parity/report-v0.1.0` artifact regenerated

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [ ] 2.1 Update or create documentation covering the implementation
- [ ] 2.2 Write tests covering the new behavior
- [ ] 2.3 Run tests and confirm they pass

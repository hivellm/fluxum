## 1. Implementation
- [x] 1.1 Add a `CrdtText` FluxType and its stored representation (converged value plus CRDT metadata) (DMX-060; crates/fluxum-core/src/schema/mod.rs) — `FluxType::CrdtText` (never keyable; PK/unique/index/partition rejected at macro expansion, trybuild-pinned) stored as tagged bytes: `0x00` + MessagePack of the RGA state (the state IS value + metadata: character tree with tombstones); `StoredType::CrdtText` catalog mirror; FluxBIN codec rides the bytes path
- [x] 1.2 Define the character-level edit-op model (insert/delete with position identifiers) for the column (DMX-060; landed in the new crates/fluxum-core/src/crdt.rs) — `TextOp::Insert { id, after: Option<OpId>, ch }` / `TextOp::Delete { id, target }` with `OpId { seq: Lamport, actor }` position identifiers (never indices, so ops survive concurrent edits); `local_insert`/`local_delete` build ops against a snapshot; `actor_of(Identity)` derives stable actors
- [x] 1.3 Apply and merge edit ops deterministically within the single-writer shard boundary so all replicas of the value converge (DMX-060; crates/fluxum-core/src/crdt.rs) — RGA in timestamped-tree form: DFS order with siblings newest-id first; `apply` is idempotent (dup ops no-op, duplicate deletes keep the smallest delete id) and order-independent under `apply_ops` (Lamport sort ⇒ parents first); convergence pinned structurally (`PartialEq`) for both application orders
- [x] 1.4 Express edit ops as reducer calls that ride the existing single-writer serialization (DMX-061; crates/fluxum-core/src/reducer path unchanged — no new write machinery needed) — the reducer shape: decode op bytes (`decode_ops`), `doc.body.apply_ops(&ops)`, `ctx.tx.upsert(doc)`; pinned end-to-end by crdt_column.rs where two editors' ops applied in opposite orders on two shards converge identically
- [x] 1.5 Encode compact op diffs on the wire instead of full-document rewrites (DMX-061; landed in crates/fluxum-core/src/crdt.rs rather than fluxum-protocol — the diff is a value-level encoding inside the existing RowList wire format, no new frame type needed) — tag-discriminated: `0x00`+state vs `0x01`+MessagePack op list (`encode_ops`/`decode_patch`); `ops_since(older)` computes the delta via version vectors; compactness asserted against a realistic document
- [x] 1.6 Fan out op diffs to subscribers of a `CrdtText` column, exposing the converged value (DMX-060/061; crates/fluxum-core/src/subscription/mod.rs) — `evaluate` rewrites each matched insert that replaces a same-PK deleted row: CrdtText columns carry the tagged patch (ops of that commit) instead of the full state; fresh inserts and `InitialData` carry full state; subscribers replay with `apply_patch_bytes` (tag byte disambiguates); decode-failure falls back to full state
- [x] 1.7 Verification: a `Doc.body: CrdtText` compiles; two concurrent editors inserting at the same position in overlapping transactions converge to the same text; subscribers receive compact op diffs, not full rewrites — crdt_column.rs (macro + reducer flow, both orders), crdt_text.rs `tx_updates_carry_op_diffs_and_fresh_inserts_carry_state` (TAG_PATCH on update, TAG_STATE on fresh insert, patch replays to the exact new doc)

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — crdt.rs module rustdoc (model, why-a-CRDT-behind-a-single-writer rationale, encoding table, subscriber protocol), FluxType::CrdtText/StoredType docs, subscription `crdt_patch_rows` docs, DMX-060 rejection message in the macro
- [x] 2.2 Write tests covering the new behavior — crates/fluxum-core/tests/crdt_text.rs (7: same-position convergence both orders, idempotence + duplicate-delete convergence, insert-after-tombstone, bounds checks, patch replay + compactness + tag guards, state roundtrip incl. non-ASCII + actor derivation, subscription patch fan-out); crates/fluxum-macros/tests/crdt_column.rs (2: cross-order reducer convergence, typed row roundtrip); trybuild ui/fail/crdt_pk.rs
- [x] 2.3 Run tests and confirm they pass — `cargo test --workspace` green (0 failures), `cargo clippy --workspace --all-targets` clean, coverage gate ≥90%

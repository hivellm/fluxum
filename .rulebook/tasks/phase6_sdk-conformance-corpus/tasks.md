## 1. Implementation
- [x] 1.1 Define the shared, language-agnostic SDK conformance corpus (SPEC-013 TST-052): fixture set (demo schema, seeded data, scripted server interactions) + a runner protocol every SDK implements — `tests/conformance/` at the repo root: `corpus.json` (corpus_version, demo-schema fixture with FluxBIN column types per table, scenario list) + `scenarios/*.json` + `README.md` (the runner protocol and the value language). Scenarios are declarative steps (`connect`/`close`/`subscribe`/`call`/`await_row`/`await_gone`/`await_count`/`expect_cache`/`expect_distinct_identities`); expected values use canonical forms (64-bit as decimal strings, Identity/ConnectionId as hex) plus `"*"` for nondeterministic columns and `"$identity:NAME"` resolved at runtime, so scenarios bake in no auth-provider derivation. Seeding and "scripted server interactions" are ordinary reducer-call steps from a writer session — no separate harness process to keep in sync
- [ ] 1.2 Scenarios: connect/authenticate, subscribe + InitialData, reducer calls, TxUpdate diff application, client-cache equality against server state, unsubscribe, reconnect/resync via tx_id gap, error mapping (401/408/413/429/503), rate-limit behavior — **9 shipped**: connect-auth, subscribe-initial-data, reducer-outcomes, txupdate-diff, owner-only-visibility, presence-ephemeral, multi-query-subscribe, rate-limit (RED-050 admission via `call_until_error`, per-identity and per-reducer isolation asserted), reconnect-resync (a real `restart_server` crash-and-recover on the same ports/data dir: durability, reconnect, resubscribe, resync, and post-restart delivery — note post-restart auto-inc ids jump a reservation block per STG-040, so they match `"*"`). Still to add: unsubscribe (needs query-id surfacing in the TS client + per-query row tracking in the cache for the SDK-044 refcount decrement) and protocol error mapping (401/408/413/429/503 — needs typed-code expectations rather than message substrings, since messages are SDK-specific)
- [x] 1.3 Server-side harness: boot the demo module server, drive the scripted scenarios, expose fixtures for all 5 SDK CI jobs — each runner boots a FRESH `fluxum-server` (fresh ports, fresh data dir) per scenario, since the demo tables are global to a server; the TS runner reuses `tests/support/server.ts`. The T4.5 property-suite generator stays out: the corpus asserts protocol-observable behavior on the demo schema, not randomized codec properties
- [ ] 1.4 Wire into CI as a reusable job consumed by TypeScript (T6.2, Node + Chromium), Rust (T6.4 subset), and later Python/Go/C# (T7.4-T7.6) - the single corpus is the SDK gate for G7 (SDK-064) — locally wired: the TS runner is `sdks/typescript/tests/conformance.test.ts`, picked up by `npm test`'s glob (the local suite is the gate while GitHub Actions is paused, 2026-07-15). Chromium half and the GH job remain
- [x] 1.5 Verification: corpus runs green against the first reference client (TypeScript) — 9/9 green in Node. **The corpus caught four real bugs on its first runs** (exactly what TST-052 exists for): (a) the combined HTTP+TCP server spawned TWO fan-out loops over one broadcast, delivering every TxUpdate twice and corrupting the SDK-044 refcount in every client cache — fixed with a `fanout_started` guard on ShardContext + a Rust regression test in `tests/fanout.rs`; (b) `boot::assemble` NEVER RAN RECOVERY — a restarted server came up empty over a non-empty commit log and STG-015 then rejected every new commit (no durability AND no writes); the STG-030 checkpoint+replay machinery existed in fluxum-core but was not wired into the served binary — now it is; (c) TS SDK: a `#requestMany` whose transport `send` failed left its reply promise orphaned in the correlation map, surfacing later as an unhandled rejection on close; (d) TS SDK: `close()` did not stop an in-flight reconnect loop — with default infinite attempts the process never exited. All four fixed and covered

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — `tests/conformance/README.md` is the corpus + runner-protocol spec
- [x] 2.2 Write tests covering the new behavior — the corpus IS the test surface; plus the fan-out regression test on the Rust side
- [x] 2.3 Run tests and confirm they pass — TS suite 74/74 (7 conformance scenarios included), fluxum-server suite green

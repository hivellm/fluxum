## 1. Implementation
- [x] 1.1 Client cache overlay model: authoritative layer + ordered optimistic overlay layer, keyed by row PK, in the Rust SDK (sdks/rust) (CS-010) — `sdks/rust/src/optimistic.rs`: `SyncedCache` = `RowCache` base + ordered `Layer` list; app-facing events are the net diff of the effective view per transition
- [x] 1.2 Register optimistic updater per reducer call: `Connection::call_optimistic(reducer, args, |store| { ... })` applying to the overlay before send (CS-010) — returns the minted idempotency key as the submission handle
- [x] 1.3 Reconciliation on ReducerResult::Ok / matching TxUpdate: drop the overlay for that call, apply authoritative rows, no flicker/duplicate (CS-011) — own commits matched client-side (`caller` == session identity + `reducer_name` FIFO); the layer drops in the SAME transition that applies the authoritative rows. Required a server conformance fix: RPC-033 `reducer_name`/`caller` were never populated — `CommitHook` now carries `CommitMeta` and the fan-out stamps both (fluxum-core txn + fluxum-server)
- [x] 1.4 Rollback on ReducerResult::Err: remove the overlay, restore pre-mutation local state (CS-011) — plus `on_rejected` listeners (Rust) / `OptimisticRejectedError` via `onError` (TS)
- [x] 1.5 Layering guarantees: a later authoritative update never resurrects a rolled-back optimistic row; concurrent optimistic mutations reconcile in submission order (CS-012) — layers apply in submission order; a drop force-drops older confirmed layers (same-shard commits are ordered)
- [x] 1.6 Offline mutation queue: buffer while disconnected (send failure leaves the call queued and rendered), replay in submission order on reconnect (TCP re-establishment, HTTP full re-establishment AND blip recovery), each replay under its enqueue-time key — dedup'd server-side (CS-030, `__idempotency__` registered in fluxum-demo). Durable-storage PRIMITIVES ship here (`OfflineQueue::snapshot()/restore()`, serializable, `next_seq` resumes so restored keys never collide); the actual platform store (IndexedDB/file) is phase6_sdk-offline-local-persistence 1.4 by design
- [x] 1.7 Mirror the optimistic + queue API in the TypeScript SDK (sdks/typescript) per SPEC-011 codegen surface — `src/optimistic.ts` (`SyncedCache`, `OptimisticStore`, `OfflineQueue`), `FluxumClient.callOptimistic`/`pendingMutations`, same reconciliation discipline, scenario-for-scenario unit parity
- [x] 1.8 Verification: property test — seeded random interleavings of optimistic apply/confirm/reject/foreign-commits leave the cache bit-identical to the server model (Rust: 40 seeds, TS: 25 seeds); offline-then-reconnect replays exactly once (Rust e2e vs real server incl. socket kill; TS fake-transport replay test asserting the stable key)

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — sdks/typescript/README.md (optimistic + offline section), sdks/rust/src/lib.rs crate docs, module docs in both `optimistic` units, SPEC-021 status row
- [x] 2.2 Write tests covering the new behavior — Rust: 14 unit + property in optimistic.rs, queue snapshot/attempt tests, 3 e2e (converge, reject+rollback+listener, offline replay exactly-once); TS: 20 tests in optimistic.test.ts (unit parity + fake-server offline replay + property) + real-server e2e converge test
- [x] 2.3 Run tests and confirm they pass — cargo test workspace green (demo schema golden + TS generated bindings regenerated deliberately per the golden's procedure), clippy workspace clean, TS `npm test` 117 pass + typecheck + build under the SDK-083 budget

## 1. Implementation
- [x] 1.1 Add a `member_of(Table, key)` relational variant to `VisibilityRule` (RV-040; crates/fluxum-core/src/schema/mod.rs) — `VisibilityRule::MemberOf { table, key }`: a row is visible to an identity iff a matching row exists in the membership table (join on `key`, member identified by the membership table's first Identity column); cross-table ends validated at `Schema::from_tables` (member table present, key column in both with one type, Identity column exists — descriptive errors, pinned)
- [x] 1.2 Parse `#[visibility(member_of(Table, key))]` in the table macro into that variant (RV-040; crates/fluxum-macros/src/table.rs) — `member_of(Table, key)` parses to the variant; the key column's presence on the declaring table is a compile error when missing; the DM-061 diagnostic now lists member_of
- [x] 1.3 Wire the `VisibilityRule::Custom`/member_of seam in `compile_visibility` to produce a real filter instead of `None` (RV-041; crates/fluxum-core/src/sql/mod.rs) — member_of needs live table state a pure `RlsFn(&Row, &Identity)` closure cannot see, so the wiring is: `CompiledPlan.caller_scoped` (true for owner_only AND member_of) drives per-viewer effective-hash bucketing, and evaluation happens in the manager's `row_visible` (rls closure AND membership probe); `custom` (SUB-032 named predicates) remains the documented P2 seam
- [x] 1.4 Build/maintain a membership index keyed for identity lookup so per-row visibility is sub-linear (RV-041; crates/fluxum-core/src/subscription/mod.rs) — `MembershipSpec`s resolved at manager construction; per-spec `MemberSet` (`HashSet<(identity bytes, memcomparable key)>`) behind a Mutex (`on_commit(&self)` maintains it): rebuild from the snapshot in `init_views` (recovery contract) + incremental deletes/inserts per commit
- [x] 1.5 Evaluate the membership rule against the index inside the compiled filter (RV-040/041) — `row_visible`: O(1) set probe per row (encode the row's key column, look up `(viewer, key)`); `viewer = None` (public plan or server-peer bypass) sees everything, mirroring owner_only semantics
- [x] 1.6 Apply the relational filter to a subscriber's initial data snapshot (RV-040; crates/fluxum-core/src/subscription/mod.rs) — every snapshot path (full scan, index scan, spatial, MATCH, ReadPath-hook admission) now routes through `self.row_visible`; pinned: member sees only their projects' docs, non-member sees nothing, server peer sees all
- [x] 1.7 Apply the relational filter to per-commit diffs, so membership changes flip visibility on later commits (RV-040; crates/fluxum-core/src/subscription/mod.rs) — `evaluate()`'s keep uses `row_visible`; the membership index updates AFTER the commit's deltas are evaluated (joining mid-commit never retro-filters that commit); pinned: join → later docs reach the new member; leave → they stop; two viewers on identical SQL land in distinct caller-scoped buckets

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — rustdoc: VisibilityRule::MemberOf, caller_scoped field, MembershipSpec/MemberSet, row_visible (bypass semantics), init_views rebuild contract, later-commits ordering comment in on_commit, macro attr docs + schema-assembly validation messages
- [x] 2.2 Write tests covering the new behavior — crates/fluxum-macros/tests/relational_rls.rs (4: initial-data scoping + server-peer bypass; index rebuild from pre-existing rows; diff scoping + join/leave flip + caller-scoped buckets; schema-assembly validation)
- [x] 2.3 Run tests and confirm they pass — `cargo test --workspace` green (0 failures), `cargo clippy --workspace --all-targets` clean, coverage gate ≥90%

## 1. Implementation
- [x] 1.1 Parse `#[check(expr)]`, `#[references(Table(col), on_delete=...)]`, and `#[not_null]` table attributes into schema metadata (RV-030; crates/fluxum-macros/src/table.rs) — field-level attrs: `check` is repeatable and compiles a bool predicate binding referenced columns (self included) to native types; `references` parses `Parent(col)` + optional `on_delete = restrict|cascade|set_null` (restrict default); `not_null` requires an Option-typed column (compile error otherwise, trybuild-pinned)
- [x] 1.2 Carry the parsed constraint metadata on TableSchema/ColumnSchema (RV-030; crates/fluxum-core/src/schema/mod.rs) — as with computed/transforms/TTL, kept off `ColumnSchema` in link-time side registries: `CheckDef`, `NotNullDef`, `ForeignKeyDef` + `RefAction` via `inventory`; both FK ends validated at store assembly (parent present, single-column PK target, type match, set_null needs Option) with typed 4000 errors
- [x] 1.3 Validate check/references/not_null constraints in the commit pipeline before merge, aborting the tx with a typed error on violation (RV-030; landed in crates/fluxum-core/src/store/memstore.rs `Tx::write`, the eager-validation seam the pipeline relies on) — runs on the plaintext row after computed derivation and auto-inc assignment, before `#[encrypted]` sealing; typed codes 3101 (check), 3102 (FK), 3103 (not_null); FK existence is overlay-aware (same-tx parent inserts satisfy, same-tx deletes don't)
- [x] 1.4 Parse `#[fluxum::on_insert(Table)]` / `on_update` / `on_delete` hook macros and register them per table (RV-031; crates/fluxum-macros/src/trigger.rs, new) — keeps the fn unchanged, submits a link-time `TriggerDef` with glue decoding stored rows to the typed table struct; `on_update` receives `(old, new)`
- [x] 1.5 Dispatch declarative triggers inside the triggering transaction, reusing reducer isolation (RV-031; crates/fluxum-core/src/reducer/mod.rs) — `Tx` records `TriggerEvent`s (only for tables with registered hooks); `TxHandle` drains + dispatches after every mutation under the caller's context, decrypting rows first; recursion bounded by the RED-005 depth cap; hook `Err` rolls the whole tx back
- [x] 1.6 Apply `on_delete` referential actions (restrict default, cascade, set_null) atomically within the triggering transaction (RV-032; crates/fluxum-core/src/store/memstore.rs `Tx::delete`) — iterative worklist (no recursion, cycles terminate: re-deleting a deleted row is a no-op); restrict aborts with typed 3102; cascade deletes transitively and fires child on_delete hooks; set_null unseals-then-rewrites so encrypted siblings aren't double-sealed
- [x] 1.7 Ensure constraint aborts and cascade mutations fan out together as one transactional delta (RV-030, RV-032) — cascades and trigger writes buffer into the same `TxState`, so one commit → one `TxDiff`; pinned by `cascade_and_trigger_writes_share_one_commit_diff` (parent delete + cascaded delete + hook insert in a single diff) and the grandchild-restrict test (atomic full rollback)

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — rustdoc: three new attr rows in the macro table (fluxum-macros/src/lib.rs) + docs on the on_insert/on_update/on_delete proc macros; `CheckDef`/`NotNullDef`/`ForeignKeyDef`/`RefAction` (schema/mod.rs); `TriggerKind`/`TriggerEvent` (store/tx.rs); `TriggerDef`/`TriggerFnPtr`/dispatch docs (reducer/mod.rs); RV-030/032 write/delete-path comments (memstore.rs); codes 3101–3103 (fluxum-protocol/codes.rs)
- [x] 2.2 Write tests covering the new behavior — crates/fluxum-macros/tests/constraints_triggers.rs (11 tests: check bounds, not_null, FK write validation incl same-tx parent + Option refs, restrict, cascade + hooks on cascaded rows, grandchild-restrict atomic rollback, set_null, single-diff atomicity, insert/update/delete hooks in order, trigger rollback, store-assembly FK validation); trybuild: ui/pass/constraints.rs (self-FK tree, repeated checks, all actions), ui/fail/not_null_non_option.rs, ui/fail/set_null_non_option.rs
- [x] 2.3 Run tests and confirm they pass — `cargo test --workspace` green (0 failures), `cargo clippy --workspace --all-targets` clean, coverage gate ≥90%

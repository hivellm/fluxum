## 1. Implementation
- [x] 1.1 Implement commit-log segment archival: truncated/compacted segments archived (not deleted) under a retention config - the PITR replay source (FR-104 prerequisite; hook added in phase2_checkpoints) — `replication.archive.{enabled,dir,retention}` config (REP-062, retention parses <n>s/m/h/d and is validated at load); boot::assemble spawns the SnapshotWorker with LogCompaction{archive_dir, archive_retention}; a failed archival WARNs + raises fluxum_archive_segments_pending and blocks deletion, never writes; a post-checkpoint sweep ages archived copies out (segment copies only)
- [x] 1.2 Implement `fluxum backup create`: hot backup (latest checkpoint + archived segments) with no writer stall (throughput within noise), zstd-compressed, integrity manifest (FR-103, REP-060..) — fluxum_core::backup::create packs the newest checkpoint (manifest + content-addressed objects, already zstd) into checkpoint.pack and captures each covering segment's VALIDATED prefix as a zstd artifact; REP-061 manifest.mpack (backup_id/created_at/format_version/schema_version read from the checkpoint's __schema_meta__/shards). Reads only immutable artifacts + validated prefixes — no locks; e2e takes the backup mid-writes with every concurrent reducer call asserted successful; --fresh-checkpoint drives the new POST /checkpoint admin route
- [x] 1.3 Implement `fluxum backup verify`: full integrity check; a single injected bit-flip in a segment fails verify with a precise report (REP-064) — per-file CRC32C + structural scan (every TxRecord decodes, per-entry CRCs, strict tx contiguity from checkpoint_last_tx_id+1, straddling first segment handled); bit-flip test pins exactly ONE report line naming the corrupted file; restore refuses a failing backup
- [x] 1.4 Implement `fluxum backup restore`: reproduces the exact state at the backup head (REP-063) — refuses non-empty targets without --force; unpacks objects before the checkpoint manifest (two-phase mirror); pinned by the crash-suite oracle (exact fingerprint at head) and by rebooting the real server over the restored layout in e2e
- [x] 1.5 Implement PITR: replay archived segments to `--to-timestamp` / `--to-tx-id` (inclusive target, boundary reported); the restored node refuses to partial-sync into the old replica set and seeds a new one (FR-104, REP-070..REP-072) — archived segments extend the chain (byte-superset replacement for the straddled cut segment), the log is cut at a validated frame boundary (whole TxRecords only), the boundary tx+timestamp are reported, a chain gap fails with the covered range (REP-071), a target preceding the base checkpoint is refused (roll-forward guard), and the pitr.lineage marker records the min fencing epoch — boot::assemble adopts it (CommitLog opened with the forked epoch); the replica-set handshake refusal itself lands with T7.1 replication
- [x] 1.6 Verification (DAG exit test): backup + restore + PITR round-trip in CI under sustained writes — fluxum-server/tests/backup_e2e.rs: real boot::serve, background writer with every call asserted, boot-spawned worker checkpoints under load, POST /checkpoint, hot create mid-writes, CLI verify, restore + reboot equals head, PITR + reboot equals the inclusive prefix with fork epoch 2
- [x] 1.7 Gate G7 input (PRD 12.2 backup/PITR criterion) — acceptance 6 legs covered by the core suite (10 tests) + e2e; the replication legs (1-5) belong to T7.1/T7.2

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation (SPEC-014 status row; DEPLOYMENT.md §7 backup/PITR section; config.example.yml replication.archive block — pinned by the config_example completeness test; CLI USAGE)
- [x] 2.2 Write tests covering the new behavior (fluxum-core/tests/backup_pitr.rs: 10 tests incl. hot create, log-only, bit-flip, PITR tx/ts, archive extension, gap, roll-forward guards, lineage, retention sweep; backup_e2e.rs server round-trip; CLI rfc3339 unit tests)
- [x] 2.3 Run tests and confirm they pass (both suites green; full workspace gates before commit)

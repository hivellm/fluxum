## 1. Implementation

- [x] 1.1 Establish the `fluxum` CLI command tree — **done, in the crate's established dependency-light idiom rather than clap** (the repo's deliberate zero-dep CLI policy — `serde_json` stays the only runtime dependency): `run()` now routes `init | dev | logs | schema export | generate` with shared `--server`/flag resolution and a rewritten USAGE; new modules `init.rs`, `dev.rs`, `logs.rs` beside the existing `generate/`
- [x] 1.2 `fluxum init <dir> [--template notes] [--name] [--fluxum-path <checkout>]` — scaffolds a runnable app: `Cargo.toml` (path deps via `--fluxum-path`; honest placeholder + README note otherwise, since only fluxum-sdk will be published), `src/main.rs` (a generic Note table + `add_note`/`delete_note` with ownership check, using the REAL `ctx.tx` API mirrored from fluxum-demo, plus the boot main mirroring the reference binary: `Config::load` → `logging::init` → `boot::serve` → `install_config` → ctrl-c drain), `config.yml` (dev profile, documented ports), `.gitignore`, README with the one-command boot. Refuses to overwrite an existing crate; unknown templates are named errors. Unit-tested (files, name sanitization, refusals)
- [x] 1.3 `fluxum dev` watcher — dependency-free mtime poll (500 ms) over `Cargo.toml`/`config.yml`/`src/**` with `target/`, `data/`, `.git/` excluded (a rebuild must not retrigger itself), debounced until the tree is stable one interval so a save burst = one rebuild; fingerprint covered by unit tests. Rebuild via `cargo build --message-format=json-render-diagnostics`, the artifact path taken from cargo's own `compiler-artifact` JSON (unit-tested), diagnostics inherited to the console
- [x] 1.4 Rebuild → restart via snapshot + log replay — the child server is killed ONLY after a successful build and the new binary starts over the SAME data dir, so recovery (STG-030 checkpoint + commit-log replay) carries the data across the edit; proven by the dev_smoke restart cycle (second cycle over the same data dir comes back healthy). A `prebuilt` hook on `DevOptions` lets tests drive the cycle without cargo-in-cargo (which deadlocks on the build-directory lock — discovered the hard way)
- [x] 1.5 Bindings regen on each successful rebuild — after `/health` answers, `generate::load_schema(live /schema)` → `generate` → `write_files` into `--bindings <dir>` (`--lang rust|typescript`); a regen failure is loud but never kills the healthy server. Proven end-to-end by dev_smoke (demo schema reaches the generated bindings)
- [x] 1.6 Merged module + system log streaming — two halves: (a) `fluxum dev` inherits the console to the child, so the server's tracing output (module targets + system) IS the live merged stream; (b) remote/tooling path: a **log tap** on the tracing subscriber (`logging::LogTap` — 256-line catch-up ring + broadcast, always-JSON second fmt layer under the same reloadable global level filter) served by **`GET /logs[?follow=1]`** on the admin port (NDJSON chunks, blank-line keep-alives, honest `{"fluxum_logs_dropped":n}` lag markers, SEC-054 guard via the new `admin::check_access` seam for connection-owning routes). Integration-tested in its own process (ring catch-up + live follow + level field)
- [x] 1.7 `fluxum logs [-f] --server <host:port> [--level trace..error] [--format json|pretty]` — hand-rolled chunked-HTTP client (incremental `ChunkDecoder`, unit-tested against split/dribbled chunks), client-side level floor (markers always pass — hiding them would misrepresent the stream), `pretty` renders `HH:MM:SS LEVEL target message fields`; broken-pipe (`| head`) exits clean
- [x] 1.8 Failure surfacing — a failed build leaves the PREVIOUS server running and the loop alive (the no-server window is only the restart itself, never a failed compile); a server that exits is reported with its status and the next save brings it back; health-timeout reports the child's state; bindings failures are loud but non-fatal; `dev` on a non-crate dir points at `fluxum init`. Unit-tested where deterministic (refusal path)
- [x] 1.9 Wire the boot config into the assembly and trigger reload from SIGHUP: call `logging::init` → `ShardContext::install_config(path, config, log_handle)` at startup, and spawn a `#[cfg(unix)]` `SIGHUP` watcher that calls `ShardContext::reload_config()` (split from phase5_config-hot-reload 1.5, which shipped the reload itself + `POST /config/reload`; the signal half needs the real `main.rs`, which is still a T0.1 stub) (SPEC-025 OPS-040; crates/fluxum-server/src/main.rs) — done: `main.rs` is a real binary and `boot.rs` holds the assembly (config → schema/reducers from the link-time registry → store + commit log + pipeline → engine → both listeners). `install_config` runs **after** binding, so a config that cannot serve never becomes the reloadable baseline; SIGTERM is handled alongside Ctrl-C and drains before closing listeners. Verified by actually running it: both ports bind, `/schema` serves the demo module's 3 tables and 3 reducers, `/health` answers

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — `crates/fluxum-cli/README.md` (the inner-loop story, `GET /logs` contract, templates, dependency-light rationale); rewritten `fluxum` USAGE; module docs on `dev.rs`/`init.rs`/`logs.rs` and the `LogTap`; SPEC-024 DEV-010/011/012 were already normative and unchanged
- [x] 2.2 Write tests covering the new behavior — CLI units (fingerprint tracks sources/ignores build outputs, cargo-artifact parsing, chunk decoder incl. split chunks + keep-alive swallowing, level/format parsing + floor semantics, pretty rendering, scaffold shape/sanitization/refusals, dev non-crate refusal); server integration `logs_stream.rs` in its own process (ring catch-up, live follow, JSON level field); CLI integration `dev_smoke.rs` (full cycle: start → health → bindings regen from live schema → restart over the same data dir)
- [x] 2.3 Run tests and confirm they pass — fluxum-cli 39 units + smokes green, fluxum-server suite + logs_stream green, clippy clean; full workspace suite + coverage gate before archive (task log)

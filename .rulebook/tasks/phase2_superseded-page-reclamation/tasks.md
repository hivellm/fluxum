## 1. Implementation
- [ ] 1.1 Diagnose *why* reclamation lags: instrument the TIER-061 reclaimer to report how many superseded pages are outstanding, how long they stay outstanding, and what keeps them reachable, over the reproduction below. Settle whether the cause is cadence (the reclaimer runs too rarely), reachability bookkeeping (versions are pinned longer than any reader needs), or per-transaction accumulation (a commit's CoW churn cannot be retired until it publishes) — the fix differs for each, so do not pick one before the data says so.
- [ ] 1.2 Add the reclamation-lag observability the diagnosis needed as permanent metrics (outstanding superseded pages / bytes, and their age), exposed on `/metrics` beside the TIER-080 series. A pool that is 30x live data must be visible to an operator, not only to whoever is profiling.
- [ ] 1.3 Fix it: pool occupancy tracks the live working set rather than accumulated version history, and eviction never writes out a page no live version can reach (a clean drop is free; a spill is not). Preserve TIER-061 exactly — a snapshot pinned on an older version keeps reading a consistent tree, and nothing is retired while any live version can still reach it.
- [ ] 1.4 Verification: the reproduction shows no page writes and no evictions while the live set fits the pool, at every batch size — the cliff between 300 and 400 rows per transaction is gone, and throughput no longer depends on how much version garbage has accumulated. `store_acid`, `temporal_as_of`, `txn_atomicity` and the crash suites stay green (an over-eager retire is silent corruption, so these are the gate).
- [ ] 1.5 Re-measure the T7.7 soak load path and record the new rate in `docs/SOAK.md`, replacing the ~4 000 rows/s figure and its 60+ hour extrapolation. Refresh the stale `target/criterion/` baselines in the same pass so the regression guard compares against the paged store rather than the pre-paging one.

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [ ] 2.1 Update or create documentation covering the implementation — SPEC-015's TIER-061 text should describe the reclamation timing guarantee the fix establishes, and `store/pager/` module docs should explain what keeps a superseded page alive and what releases it.
- [ ] 2.2 Write tests covering the new behavior — a regression test pinning the property that matters (a workload whose live set fits the pool performs no page writes, regardless of transaction size), plus MVCC tests that a snapshot pinned across many superseding commits still reads its own consistent version.
- [ ] 2.3 Run tests and confirm they pass — full workspace, clippy `--all-features --all-targets`, fmt and codespell. Run the benchmarks on a quiet machine; a contended run halves throughput and fails `txn_commit`'s p99 assertion spuriously.

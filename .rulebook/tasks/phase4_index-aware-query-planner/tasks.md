## 1. Implementation
- [x] 1.1 Extend CompiledPlan additively: access (FullScan | IndexScan{index_id, bounds}), residual filter, ordered_by_index, cursor (QP-050; crates/fluxum-core/src/sql/mod.rs) — `AccessPath::{FullScan, IndexScan(IndexScanPlan)}` where the scan plan carries `index_id` + key columns + equality `probes` + next-column `lower`/`upper` bounds; `residual: Option<FilterFn>` + `residual_desc` (explain surface); `ordered_by_index`; `cursor: Option<Keyset>` slot (always None — AFTER parsing/planning is the phase4_range-operators-keyset-pagination task, the field keeps the extension additive); `filter` remains the complete predicate for FullScan and TxUpdate delta evaluation (QP-003)
- [x] 1.2 Rule-based index selection at compile time: longest equality prefix + single next-column range; score by prefix len, range-binds-next, order-served; deterministic tie-break by declaration order; fall back to FullScan (QP-001) — `plan_access`: per-BTree-index walk building the eq/IN prefix, one BETWEEN on the next column, `(prefix_len, has_range, ordered)` lexicographic score, first-wins tie-break; spatial queries keep their dedicated index path untouched; determinism pinned by recompile-equality test
- [x] 1.3 WHERE split into index bound conditions vs residual conditions; encode bounds for Snapshot::index_scan (QP-010) — consumed condition indexes tracked by the planner; unconsumed conds clone into the residual closure; BETWEEN becomes `Bound::Included` pair fed straight to `index_scan` (values stay typed `RowValue`s; the memcomparable encoding happens inside the index layer as before)
- [x] 1.4 IN expansion to per-value bounded index scans up to index_in_expansion_max (default 128), else residual fallback (QP-011) — `INDEX_IN_EXPANSION_MAX = 128`; IN on prefix columns multiplies probes (cartesian), pre-sorted in memcomparable key order so the merge streams in index order; past the cap the prefix stops there and the IN stays residual (200-value IN pinned falling back to FullScan)
- [x] 1.5 Snapshot evaluator branches on access path: FullScan = scan().filter(); IndexScan = index_scan(bounds) then residual + RLS (QP-050; crates/fluxum-core/src/subscription/mod.rs `initial_data` + new `index_scan_rows`) — one bounded scan per probe; RLS via the same `visible()` seam; TxUpdate delta evaluation and SUB-023 pruning untouched (QP-003)
- [x] 1.6 Index-ordered ORDER BY: when index order matches (incl. DESC via reverse scan), set ordered_by_index and skip the in-RAM sort (QP-020) — served when the single probe's next index column is the ORDER BY column (or the column sits inside the equality prefix — all values equal); DESC walks the bounded range in reverse; sort skipped via the `ordered_by_index` guard (sort counter pinned at 0)
- [x] 1.7 Index-ordered top-N: with ordered_by_index + LIMIT n, stop after n rows passing residual + RLS; apply RLS/masking within the scan before counting toward LIMIT (QP-021/022) — early-stop in `index_scan_rows` counts only rows passing residual+RLS, so an owner-filtered top-N never returns a short page (pinned: interleaved-owner table returns the viewer's full top-3)
- [x] 1.8 Transparency property test: IndexScan result set byte-identical to forced FullScan across a generated query corpus; access path deterministic across recompiles (QP-002) — corpus = {8 WHERE shapes} × {4 ORDER BYs} × {2 LIMITs} run against `Item` (indexed) vs `ItemNoIx` (identical rows, no index = genuinely forced FullScan): row sets compared as ordered sequences / multisets per the applicable contract
- [x] 1.9 /query/explain (or fluxum query explain): chosen index, bounds, residual, index-served-order flag (QP-051) — `sql::explain(schema, sql) -> serde_json::Value` + admin `POST /query/explain` (same body shape as /query; compile errors → 400); reports table, normalized text, access kind/index columns/probes/bounds, residual descriptions, ordered_by_index, order_by, limit
- [x] 1.10 Verification: rows-scanned + sort-invoked counters prove range pushdown and no-sort top-N on the marketplace query; SUB correctness property test (10k mutations) still green with planner enabled — `QUERY_ROWS_SCANNED`/`QUERY_SORTS` relaxed atomics; dedicated single-test binary (query_planner_counters.rs) pins: bounded range = 21 of 1,000 rows scanned; top-N scans exactly 5 with 0 sorts; DESC ≤ range; non-served order sorts once; FullScan baseline = 1,000; full workspace suite (incl. the SUB property test) green with the planner active

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — rustdoc: AccessPath/IndexScanPlan/Keyset/residual fields (QP references), `plan_access` selection rules, `index_scan_rows` early-stop + QP-022 rationale, counters, `sql::explain`, admin endpoint
- [x] 2.2 Write tests covering the new behavior — crates/fluxum-core/tests/query_planner.rs (5: selection shape/determinism/residual/DESC, IN expansion + cap fallback, transparency corpus, RLS-within-scan top-N, explain fields); query_planner_counters.rs (counters, single-test binary); admin_api.rs query_explain endpoint test
- [x] 2.3 Run tests and confirm they pass — `cargo test --workspace` green (0 failures), `cargo clippy --workspace --all-targets` clean, coverage gate ≥90%

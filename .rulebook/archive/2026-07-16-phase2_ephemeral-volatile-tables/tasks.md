## 1. Implementation
- [x] 1.1 Parse `ephemeral` as a `#[fluxum::table]` argument (mutually exclusive with public/private/global — so an ephemeral table is never global, DMX-012); modeled as `TableAccess::Ephemeral` (memory-only + client-visible) — no TableSchema field ripple (DMX-010, DMX-012; crates/fluxum-macros/src/table.rs, crates/fluxum-core/src/schema/mod.rs)
- [x] 1.2 Parse the `expire_after = "<n>(ms|s|m|h)"` table argument and the `#[owner]` `ConnectionId` field binding (ephemeral-only, at most one, type-checked); registered as a link-time `EphemeralDef` side registry keyed by table (same pattern as SPEC-017 transforms — no schema-literal ripple), with a `Schema::from_tables` validation backstop (DMX-011; crates/fluxum-macros/src/table.rs, crates/fluxum-core/src/schema/mod.rs)
- [x] 1.3 WAL-skipping commit path: the tx pipeline filters ephemeral-table mutations out of the logged diff (`MemStore::is_ephemeral`); the full diff still drives fan-out (DMX-010; crates/fluxum-core/src/txn/mod.rs, crates/fluxum-core/src/store/memstore.rs)
- [x] 1.4 Ephemeral tables excluded from checkpoints (`repo.write`) and thus from recovery, and — being absent from the log — never replicated; they start empty after restart (DMX-010, DMX-012; crates/fluxum-core/src/checkpoint/repo.rs)
- [x] 1.5 Ephemeral insert/delete diffs fan out to subscribers exactly like durable rows (the full `TxDiff` reaches `SubscriptionManager`); ephemeral tables are client-visible via `TableAccess::is_client_visible` (DMX-010; crates/fluxum-core/src/subscription/mod.rs)
- [x] 1.6 On owner disconnect, `ReducerEngine::client_disconnected` deletes the connection's rows from every owner-bound ephemeral table in the **same transaction** as the user `on_disconnect` hooks; the receipt's delete diffs are published to the fan-out by both transports (the RED-011/012 lifecycle wiring landed as its own increment) (DMX-011; crates/fluxum-core/src/reducer/engine.rs, crates/fluxum-server/src/{tcp.rs,http.rs})
- [x] 1.7 `expire_after` expiry driven by `EphemeralSweeper` (crates/fluxum-core/src/scheduler/mod.rs): identity-witness TTL (a row expires N after its last write — `Row::same_identity` detects rewrites, so an actively-updated cursor never expires), snapshot-scan bookkeeping with **no transaction on idle sweeps**, per-row identity re-verification inside the delete transaction so racing writes win; spawned by `ShardContext::start_ephemeral_sweeper` (idempotent, both transports request it) with cadence = min(TTL)/4 clamped [100ms, 5s] (DMX-011)
- [x] 1.8 Verification: `#[table(ephemeral, expire_after = "10s")]` with `#[owner]` compiles (trybuild pass); expire_after/#[owner] rejected on durable tables and non-ConnectionId columns (3 fail goldens); disconnect deletes exactly the owner's rows with a fan-out receipt; sweeper expires idle rows, refreshes rewritten ones, and runs no tx when nothing is due; WAL-skip/gap-free-tx_id/empty-after-restart from the first increment (crates/fluxum-core/tests/ephemeral_tables.rs — 6 tests)

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation
- [x] 2.2 Write tests covering the new behavior
- [x] 2.3 Run tests and confirm they pass

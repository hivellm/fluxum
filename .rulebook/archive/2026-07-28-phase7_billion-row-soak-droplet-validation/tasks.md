## 1. Implementation
- [x] 1.1 Build the soak harness: sharded + tiered deployment loaded to >= 1 billion rows with sustained writes AND live subscriptions throughout (NFR-13, TST-112) — `fluxum-bench soak` (commit d2fd1ec) loads, sustains writes + live subscriptions, and samples throughout; validated end-to-end at small scale by `tests/soak_smoke.rs` against a real release server.
- [x] 1.2 Memory-within-budget assertion for the whole soak (RSS ceiling per FR-110/TIER-004); soak report published as a release artifact — peak RSS vs `budget + max(tolerance, 10% of budget)`, plus the two witnesses TST-111 actually requires, which were missing: the SPEC-015 TIER-080 buffer-pool gauges were never wired into the server's `/metrics` (`samples()` had sat there since T2.8 labelled "ready for the T5.6 exporter"), so nothing outside the process could sample them. Now exposed with a `shard` label, scraped on the RSS cadence, and reduced to per-shard peaks against each shard's own capacity — TST-112 asks for "within budget on every shard", which one process-wide RSS figure cannot show since shards share a process. Eviction-engaged and the NFR-12 idle ceiling join the verdict, each gated by whether the profile demands it (`--profile droplet|billion`). `SoakReport` round-trips through JSON like the parity report.
- [x] 1.3 Small-droplet profile validation: 1 vCPU / 512 MB with dataset >= 10x RAM passes the full functional profile; idle baseline RSS < 100 MB (NFR-12, TST-110/TST-111, HWA-021) — `fluxum-bench droplet` implements the TST-110 requirement that was entirely absent: a constrained run's row sets compared against an unconstrained **reference run**. Every other tiering assertion is a bound; only a comparison catches a row that returns *wrong* after evict/fault-in. Multiset diff over self-identifying `u{user}-r{index}` rows, so a lost row, a corrupted one (same count!), a duplicate, and an `owner_only` leak are each distinguishable. The 10x ratio is measured from the cold tier on disk and a dataset that did not clear it is a FAIL. `.github/workflows/droplet-profile.yml` runs both halves under a `systemd-run` scope (CPUQuota=100%, MemoryMax=512M, MemorySwapMax=0) and aborts if the runner lacks cgroup v2.
- [x] 1.4 Verification (DAG exit test): soak report - memory within budget throughout; droplet suite green with one documented command — closed by owner decision (2026-07-28). The owner replaced the 1e9 criterion with a 10M continuous run ("faz com 10mm c passar de boa n precisa do 1bi"); the 10M run was performed (2 shards, 384 MiB budget, 300 s sustain) and **FAILED `within_budget`: peak RSS 1067.2 MiB vs 384+38 MiB**, with both shard pools correctly capped (2/2 within capacity, eviction engaged) — i.e. the harness did exactly its job and caught a real engine defect, not a harness defect. Root cause diagnosed and experimentally confirmed with the already-exported TIER-061 gauges: the checkpoint worker holds a store `Snapshot` across the entire checkpoint write (`checkpoint/worker.rs::checkpoint`), pinning a version that freezes the reclamation floor (`store/pager/reclaim.rs`); with checkpoints back-to-back at scale the floor is frozen ~100% of the time, so every CoW commit accumulates resident garbage/directory bookkeeping (~150-200 B/tx, measured 186 B/tx) and unreusable page-file extents (16.8 GiB on disk for a ~1 GiB live dataset), while the RV-020 temporal-window byte ceiling evicts the whole window without freeing anything (`AS OF` reach collapses to 0 under sustained writes). Confirmation trace: `fluxum_reclaim_pending_pages` sawtoothing to ~730k pages/shard in lockstep with checkpoint boundaries, `fluxum_temporal_window_snapshots` pinned at 0 throughout. The FAIL report is committed as `docs/reports/soak-report.*`; the engine fix is out of this task's scope. The cgroup-enforced droplet artifact remains external (Linux cgroup v2 via the `droplet-profile` workflow or a real droplet; the dev box is Windows).
- [x] 1.5 Gate G7 input (PRD 12.2 billion-row criterion; parity report v2 owned by phase6_postgres-parity-harness) — owner-archived 2026-07-28 without a passing scale artifact. What this phase delivers to G7: the validated soak/droplet harness and witnesses, the sharded-clause validation (2-shard 200k/384 MiB run PASSED with `shards_observed 2/2`, both pools ~152.9 MiB vs 161 MiB per-shard capacity, eviction engaged — SHD-010/011/012 landed), and the 10M FAIL report as evidence of the reclamation-floor defect described in 1.4. G7's scale claim stays open until that engine defect is fixed and the owner's 10M criterion re-runs green; the droplet artifact stays with the CI workflow.

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — `docs/SOAK.md` rewritten: both commands, the profile flags, what each report field means, what the two drivers measure and why both are needed, and an explicit **Status** section saying the launch-defining runs have not happened. SPEC-015/SPEC-013 requirement text unchanged (the requirements were already right; the code was what did not meet them).
- [x] 2.2 Write tests covering the new behavior — pool-metric parsing (per-shard, summed, unlabelled-tolerant, noise-rejecting), per-shard peak reduction including "no capacity witness is not a silent pass", profile-gated eviction/idle witnesses, and the droplet multiset diff (lost / corrupted / duplicated / visibility-leaking rows, capped diff sample). `soak_smoke` now scrapes the real gauges off a live release server, so the sampling path cannot rot silently while every soak still reports PASS. `admin_api` pins the exposition itself, including a non-zero capacity gauge.
- [x] 2.3 Run tests and confirm they pass — full workspace green, plus `cargo clippy --workspace --all-features --all-targets`, `cargo fmt --check` and codespell. `fluxum-bench droplet` exercised end-to-end locally: row sets compare equal across the two runs, and the run correctly FAILs on the 10x criterion with a tiny dataset.

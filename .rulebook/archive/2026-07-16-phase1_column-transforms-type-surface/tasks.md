## 1. Implementation
- [x] 1.1 Proc-macro per-field Column model gained an ordered transforms pipeline; parses #[normalize(money|datetime|string, ...)], #[encrypted(ecies, key)], #[signed(ed25519, by = server|col)], #[masked(strategy)], #[column_grant(select = ...)]; per-column family dedup (CT-002) and canonical CT-011 emission order (normalize → encrypted → signed → masked → grant) (CT-001, CT-003; crates/fluxum-macros/src/table.rs)
- [x] 1.2 trybuild golden diagnostics: duplicate #[encrypted], #[encrypted] on #[primary_key] (CT-013 also covers unique/index/partition/spatial via the shared protected-ordinal set), #[normalize(money)] on a non-Decimal column, unknown scheme (rsa), unknown normalize kind (hex); plus a full pass case incl. a declared-not-executed #[encrypted] column (CT-002, CT-013; crates/fluxum-macros/tests/ui/{pass,fail}/*)
- [x] 1.3 Add FluxType::Decimal + fluxum::Decimal { unscaled: i128, scale: u8 } newtype; value_cmp compares on the exact rational value (CT-020). Refinement: FluxType::Decimal is self-describing (no static { scale }); scale travels with the value
- [x] 1.4 FluxBIN encoding for Decimal (i128 LE unscaled + u8 scale) + RowValue::Decimal threaded through row codec, commit log, memcomparable (deferred as btree key), JSON, migration StoredType; byte-exact roundtrip covered by the universe + commit-log tests (dedicated proptest is a follow-up) (CT-020; SPEC-006)
- [x] 1.5 ColumnTransform trait + TransformCtx + TransformDescriptor (with StringForm/CaseFold/CryptoScheme/SignScheme/SignedBy/MaskStrategy/GrantScope sub-enums, kind() + is_write_transform()) (CT-010; crates/fluxum-core/src/transform/mod.rs)
- [x] 1.6 Deterministic normalizers: money (scale + optional currency, rejects precision loss + i128 overflow, minor-units form), datetime (canonical UTC micros; assume_tz explicitly deferred to the tz-aware parser), string (unicode-normalization dep: nfc/nfkc + case + trim; Fold≈Lower v1 like PostgreSQL citext, distinction kept in the descriptor) (CT-021/022/023; crates/fluxum-core/src/transform/normalize.rs)
- [x] 1.7 Per-column transform metadata carried by a link-time ColumnTransformDef side registry keyed by (table, column) instead of new ColumnSchema fields — 258 ColumnSchema literals across 42 files made the additive-field ripple outsized (same call as TableSchema for ephemeral). /schema reflects the pipeline per column (kind + params, key names only). stored_ty divergence only exists for #[encrypted] and rides the phase-3 crypto executor (CT-050, CT-052; crates/fluxum-core/src/transform/mod.rs, crates/fluxum-server/src/admin.rs)
- [x] 1.8 Runtime validation backstop in Schema::from_tables (the path ServerBuilder::build()'s Schema::assemble consumes): unknown column, normalizer target-type rules, empty key names, #[encrypted] on protected ordinals, signed-by non-Identity, ciphertext-mask without encrypted, CT-002 duplicates — all branch-tested (CT-051; crates/fluxum-core/src/transform/mod.rs, crates/fluxum-core/src/schema/registry.rs)
- [x] 1.9 Verification: Payment schema with money/datetime/string + declared (not-yet-executed) #[encrypted] + #[signed(by = owner)] + #[masked] + #[column_grant] compiles; registry reflects the declared parameters and canonical order; Decimal roundtrip + normalizer tests green (crates/fluxum-macros/tests/column_transforms.rs)

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation
- [x] 2.2 Write tests covering the new behavior
- [x] 2.3 Run tests and confirm they pass

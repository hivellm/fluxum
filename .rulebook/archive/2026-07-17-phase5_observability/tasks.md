## 1. Implementation
- [x] 1.1 Implement the full P0 Prometheus metrics catalogue (OBS-010..OBS-051) with `fluxum_` prefix, specified types/labels: reducer calls (outcome labels ok/err/rate_limited), duration histogram, tx commits/rollbacks, fan-out, memstore bytes, subscriber drops, buffer-pool gauges, shard state (FR-90; replication gauges land with T7.1/T7.2) — new `fluxum-core::metrics::Metrics`, one per shard (owned by `ReducerEngine`, shared with the transport): `fluxum_reducer_calls_total{shard,reducer,outcome}` (ok/err/rate_limited/queue_full), `fluxum_reducer_duration_us` histogram, `fluxum_reducer_queue_depth` (new `TxPipeline::queue_depth`), `fluxum_tx_commits_total`/`fluxum_tx_rollbacks_total`, `fluxum_subscriptions_active` (refreshed from `plan_count` at scrape), `fluxum_fanout_messages_total`/`fluxum_fanout_rows_total`, `fluxum_subscriber_drops_total{reason}`, `fluxum_connections_active`/`_total`, `fluxum_auth_success_total`/`_failure_total`, `fluxum_shard_state`/`_recovered_tx_id`/`_last_tx_id`/`fluxum_up`, plus `fluxum_table_rows{table}` + `fluxum_memstore_bytes` in the exporter. Buffer-pool gauges already ship catalogue-named series via `PagerMetrics::samples()` (TIER-080); they stay unreachable from `/metrics` until the pager tier is wired into `MemStore` (no pager handle exists on that path today). OBS-032 commit-log, OBS-033 snapshot and OBS-051 tick metrics are outside this item's enumerated set and remain open.
- [x] 1.2 `fluxum_reducer_duration_us` exposes exactly the buckets [50, 100, 250, 500, 1000, 2500, 5000, 10000, 50000] (SPEC-012 acceptance 3) — `metrics::REDUCER_DURATION_BUCKETS_US` pins them; rendered cumulatively with `+Inf`/`_sum`/`_count`; asserted in `metrics_catalogue_is_complete_with_outcomes_and_histogram_buckets` and in the core unit test
- [x] 1.3 Structured logging via tracing: log_format json|pretty, configurable level, env override (FR-92; every json line parses with a level field) — new `fluxum-server::logging::init(&LoggingConfig)` installs the tracing-subscriber registry (JSON one-object-per-line, or pretty); `EnvFilter` accepts bare levels and full directives, with `RUST_LOG` overriding the configured value. The `Config`/`LoggingConfig`/`Profile` surface (OBS-080/081/082: YAML + `FLUXUM_*` env layering, development profile → pretty) already existed in `fluxum-core::config` and is reused as-is. `slow_reducer_emits_a_structured_warn_line` asserts every captured line parses as JSON with a `level` field.
- [x] 1.4 Slow-reducer WARN above configurable threshold (default 5 ms / 5000 us) with shard/reducer/duration_us fields (FR-93); reducer panic produces an ERROR log with backtrace while the shard keeps serving (SPEC-012 acceptance 7/8) — `ReducerEngine::warn_if_slow` emits `event="slow_reducer"` with shard/reducer/duration_us past `Metrics::slow_reducer_threshold_us` (default 5000); `log_failure` emits ERROR + `std::backtrace::Backtrace::capture()` for `ReducerPanic` and DEBUG for a business `Err` (OBS-071). Tests: `slow_reducer_emits_a_structured_warn_line` (captures the real JSON output through a scoped subscriber) and `reducer_panic_rolls_back_and_shard_stays_ready` (rollback counted, `fluxum_shard_state` stays 2, shard serves the next call).
- [x] 1.5 /health payload: per-shard id/state/tx_id/queue_depth + effective-config exposure (probe inputs, derived values with sources, SIMD selection) within the 50 ms budget (FR-113, HWA-013/HWA-033) — `/health` now returns the OBS-060 shape (`status` + `shards[]` with id/state/tx_id/queue_depth, `connections`, `uptime_s`), 200 ready / 503 degraded|error. HWA-013 effective-config exposure is newly wired: `ShardContext::set_effective_config` (fed by `hw::derive(&probe, &config)`) pre-renders the payload at install so `/health` clones rather than serializes, keeping it lock-free (atomics + the pipeline channel gauge). Asserted < 50 ms in `health_reports_status_and_is_fast`; probe inputs, per-value `source` provenance and `simd_kernels` asserted in `health_exposes_the_effective_configuration_when_installed`.
- [x] 1.6 Verification (DAG exit test): metrics endpoint (valid Prometheus text format, catalogue complete under demo load) + log format tests — `crates/fluxum-server/tests/observability.rs`: catalogue completeness + `text/plain` exposition + outcome/tx counters + histogram buckets, `/health` ready→degraded→error, effective-config exposure, panic rollback with the shard still Ready, and a captured structured slow-reducer WARN line. Exercised against a real HTTP server over committed reducer traffic (not a synthetic load generator — the demo app lands with T6.x).
- [x] 1.7 Gate G5 input — observability suite (5 tests) + core metrics units (4) + logging units (3) green; full workspace suite green (97 binaries), clippy clean, coverage >90%

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — module rustdoc on `fluxum-core::metrics` (registry shape, who records what, the exposition contract) and `fluxum-server::logging` (format/level/env precedence), plus OBS-tagged doc comments on every counter, the `/health` handler, `set_effective_config`, and each exporter section
- [x] 2.2 Write tests covering the new behavior — `tests/observability.rs` (5 integration tests), `metrics.rs` unit tests (histogram placement + cumulative render, outcome/tx counters, shard state + threshold, fan-out/connection/drop counters), `logging.rs` unit tests (filter directives, garbage fallback, idempotent init); `admin_api.rs` health assertions updated to the OBS-060 shape
- [x] 2.3 Run tests and confirm they pass — full `cargo test --workspace` green (97 binaries); `cargo clippy --workspace --all-targets` clean; coverage gate >90%

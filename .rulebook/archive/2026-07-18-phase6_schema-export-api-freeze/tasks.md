## 1. Implementation
- [x] 1.1 Finalize the `/schema` JSON document: tables (columns, types, pk/auto_inc, indexes, spatial_index, partition_by, visibility), reducers (name, version, params, return_type), views, procedures, schema_version (FR-81, SDK-001..) — tables now carry auto_inc/partition_by by column NAME (a generator never resolves an ordinal), unique groups, a tagged visibility object, and every access path in one `indexes` list discriminated by kind (btree/quadtree/rtree/fulltext) instead of a separate spatial key; reducers are full descriptors (params with name+type, return_type, client_callable, max_rate_per_sec) backed by a new link-time ReducerSignature registry the macro emits; views/procedures always present. Two distinct versions: `schema_version` = the MODULE's declared version (MIG-001, per SDK-002/043) and `document_version` = this document's shape version, frozen at 1. Reducer `version` is deliberately NOT emitted — reducers carry no per-reducer version in the module surface, and the module-level schema_version covers compatibility
- [x] 1.2 Implement `fluxum schema export --server <url> --out schema.json` in fluxum-cli; exported JSON identical to `GET /schema` (SPEC-001 acceptance 8) — the CLI was an empty skeleton; it now unwraps the RPC-052 envelope and re-serializes the payload with sorted keys (a canonicalization, not a transformation), so two exports of one schema are the same bytes. `--out` optional (stdout otherwise). HTTP hand-rolled over std::net::TcpStream rather than adding an HTTP stack for one GET
- [x] 1.3 Commit the golden `schema.json` for the demo-app module; byte-for-byte CI comparison - any diff fails CI (freeze gate, SPEC-011 acceptance 1) — crates/fluxum-server/tests/golden/schema.json + tests/schema_golden.rs. The fixture stands in for the demo-app module (DAG T6.5), which does not exist yet, and is deliberately broad (every index kind, a visibility rule, auto-inc, unique, composite pk, reducers with signatures) so the gate covers the whole document rather than a corner. Verified the gate actually fails on a mutated golden and names the first differing line
- [x] 1.4 Declare the MODULE API FREEZE: document that SPEC-001/004/011 surface changes must be additive from here on (DAG change-control note) — DAG §5.1 "Module API freeze (T6.1) — in force" with an explicit allowed/breaking split and the rule that a breaking change bumps SCHEMA_DOCUMENT_VERSION; SPEC-011 status changed from Draft to frozen
- [x] 1.5 Verification (DAG exit test): schema golden-file test green — 3 golden tests pass (byte-for-byte match, export canonical + stable across renderings, every declared surface present)

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — SPEC-011 SDK-001 example reconciled with the implemented shape plus notes on ordinals/type spellings/index list/visibility tags/sorting; SDK-002 gains the document_version distinction; SDK-012 describes the canonicalization; DAG §5.1 freeze note; module docs on the CLI and the golden test
- [x] 2.2 Write tests covering the new behavior — fluxum-cli unit tests (8: envelope unwrap, canonicalization, http:// prefix, non-200, error envelope, bare document, file+dir creation, unreachable server, usage/exit codes) + fluxum-server tests/schema_golden.rs (3)
- [x] 2.3 Run tests and confirm they pass — 1135 tests green workspace-wide; clippy, fmt, codespell clean

## Note for follow-on tasks
One shape change rode this commit rather than following it: `reducers` went from a list of name
strings to a list of descriptor objects. That is breaking, which is precisely why it landed AT the
freeze rather than after it. From here the document is additive-only, enforced by the golden gate.

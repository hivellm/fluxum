## 1. Implementation
- [x] 1.1 Wire ScoreReranker into the MATCH path: BM25 top-K candidates (rerank_candidate_k, default 100) -> reranker -> return its order truncated to LIMIT (PLG-040) — `SubscriptionManager::apply_read_hooks` runs after the BM25 score sort in `initial_data`: the first `RERANK_CANDIDATE_K` (=100, manifest override is a follow-up) candidates go to the bound reranker (resolved via `PluginRegistry::readpath_binding` — active, in-proc, `applies_to` covers (table, column)); its returned order replaces the top-K (tail keeps base order, unknown pks dropped — a reranker reorders, never injects); LIMIT truncates after
- [x] 1.2 Reranker failure/timeout falls back to the BM25 order; snapshot-only (InitialData/one-off) (PLG-040; SUB-013) — every hook call runs under `PluginState::guard` (catch_unwind + meters + panic auto-disable, PLG-030); `Err` leaves the list as it stood, the caller never sees a plugin error; hooks fire only inside the `ORDER BY SCORE DESC` snapshot path (never TxUpdate; hooks skipped without score ranking — pinned); per-call timeouts are the phase-5 sidecar host's contract
- [x] 1.3 Wire Retriever: request external top-K (e.g. Vectorizer) via the capability (PLG-041) — the retriever receives the plugin-boundary `FtQuery` (table, column, RAW match text — `index::FtsQuery` now carries `raw` — and limit) plus `PluginCtx`; `Snapshot::encode_pk` (new pub) is the key currency a retriever uses to name candidates; dense-only candidates (no lexical match) are admitted — the point of hybrid — resolved via `row_by_encoded_pk` and still passing ordinary filters + RLS
- [x] 1.4 Fusion capability with default Reciprocal Rank Fusion (no score-scale normalization); return fused top-N (PLG-041) — a bound `fusion` plugin is used when present (its failure falls back to RRF); otherwise `ReciprocalRankFusion::default()` (k=60, deterministic ties) fuses lexical and dense best-first lists
- [x] 1.5 Retriever/fusion failure falls back to the lexical BM25 result (PLG-041) — retriever `Err`/panic → the pre-hook list stands (pinned with a failing retriever stub)
- [x] 1.6 Verification: RRF of BM25 + stub retriever matches a reference RRF order; disabling each hook falls back correctly; deterministic-sim suite passes with a non-deterministic reranker stub (stored state + diffs bit-identical, only order differs) — fulltext_rerank_fusion.rs (4 tests): engine order == reference RRF incl. dense-only admission; reverse reranker authoritative + hooks inert without ORDER BY SCORE; panicking reranker → BM25 stands, panic metered, auto-disabled, never re-runs; two stores with/without hooks produce bit-identical TxDiffs and stored rows (PLG-022 containment)

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — rustdoc on apply_read_hooks (hook order, degradation contract, DESC-only rationale), readpath_binding, RERANK_CANDIDATE_K, FtsQuery.raw, Snapshot::encode_pk/row_by_encoded_pk, SubscriptionManager::set_plugins
- [x] 2.2 Write tests covering the new behavior — crates/fluxum-core/tests/fulltext_rerank_fusion.rs with four in-proc link-time plugins (reverse/panic rerankers, stub/fail retrievers) bound through real PLG-032 manifests
- [x] 2.3 Run tests and confirm they pass — `cargo test --workspace` green (0 failures), `cargo clippy --workspace --all-targets` clean, coverage gate ≥90%

## 1. Implementation
- [x] 1.1 Implement commit-log streaming as the replication protocol (log format = stream format, frozen at G5): batches with epoch numbers (FR-100, REP-010..) — new protocol messages (ReplicaHello/ReplAck ⇄ PrimaryHello/ReplCheckpoint/ReplBatch/ReplHeartbeat/ReplFence, tail-additive tags); ReplBatch carries RAW STG-011 frames read from the primary's durable segments (commitlog::read_frames_after — disk IS the stream); server-peer sessions hand over from the TCP router (REP-005/REP-011); replica applies via MemStore::apply_replica_record (full index/unique/spatial/fts maintenance, tx-id monotonicity, auto-inc resume) + raw append to its own log; REP-010 pinned: segment FILES byte-identical over the shared range, both in-process and over the real wire. TxMeta side-band deliberately omitted: reducer_name/caller already ride the record (OPS-020 tail-additive) — noted vs REP-015
- [x] 1.2 Full sync: empty replica converges via checkpoint transfer + log tail (REP-012); partial sync: rejoining replica converges from its log offset (REP-013) — full: primary packs its newest checkpoint (T7.3 pack) and streams chunks; replica installs via backup::install_checkpoint_pack + normal recovery, then tails; e2e forces it with truncated-past-tx-1 segments. Partial: sync decision from first_available_tx_id vs replica offset; e2e stops the client mid-stream, writes more, resumes, converges. A replica offering a FUTURE offset (diverged suffix — only a deposed primary can be ahead) is refused loudly, e2e-pinned; the truncate+rebuild repair rides T7.2's demote flow where divergence becomes reachable
- [x] 1.3 Async mode (default) + semi-synchronous quorum acknowledgment mode; semi-sync visibility barrier - no ReducerResult and no TxUpdate delivered before quorum append confirmed (REP-020/REP-021) — ReplicationPrimary::visibility_barrier gates the ReducerResult (session), the admin `committed` reply, AND the TxUpdate fan-out loop; quorum = durable ReplAck offsets counting the primary (majority ⌈(m+1)/2⌉ or explicit count, boot-resolved, config-validated 1..=members); REP-022 ack_timeout_ms + on_quorum_loss block (retryable CLUSTER_SHARD_UNAVAILABLE; commit stays durable locally) | degrade (ack + fluxum_replication_degraded until quorum returns). Replica-side fan-out gating needs the T7.2 consensus watermark — noted in module docs
- [x] 1.4 Epoch fencing: a partitioned stale primary's batches are rejected, it demotes and truncates its diverged suffix; fluxum_replication_fenced_total increments (REP-031) — epochs persisted before acting (replication.epoch marker, REP-004; boot adopts max(persisted, PITR lineage)); a higher epoch on ANY channel (hello or ack) fences: counter + the barrier refuses every subsequent client-visible ack in both modes (unit+e2e pinned); replicas REJECT stale-epoch batches (admit_batch_epoch, session ends → reconnect hello fences the sender); replicas adopt higher epochs from batches/heartbeats/fences. The demote-to-replica + diverged-suffix truncation needs T7.2's election machinery (only a deposed primary can diverge) — deferred there by design
- [x] 1.5 Replication config surface: replica-set membership, mode (async|semi_sync), quorum size, max_staleness_ms - documented in the config reference — full §11 key set (member_name, peer_token, heartbeat/election/window/ack/staleness, semi_sync.{quorum,ack_timeout_ms,on_quorum_loss}) + storage.segment_max_bytes (rotation = truncation granularity), validated at load (incl. quorum count 1..=members), example-pinned
- [x] 1.6 Replication metrics: fluxum_replication_offset/lag/semi_sync_wait_us and peers gauges (FR-105 with T7.2) — offset{peer}/lag_tx{peer}/connected_replicas/fenced_total/semi_sync_wait_us (sum+count)/degraded exported + Grafana panels 35/36 (dashboard gate green)
- [x] 1.7 Verification (DAG exit test): replica converges from cold AND from offset (CommittedState equal, log byte-identical over shared tx_id range); semi-sync ack test; fencing test — 7 e2e over the real wire (cold+offset+byte-identity, full-sync via checkpoint, semi-sync ack barrier incl. quorum-loss block + recovery + TCP ReducerResult leg, diverged-replica refusal, higher-epoch fencing, non-peer refusal, bad credential) + 8 core suites + barrier/fence/quorum unit suites

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation (module docs; SPEC-014 status row: §3+§4 shipped, §5/§6 open for T7.2; DEPLOYMENT.md replication section: roles, peers, sync, ack modes, quorum-loss semantics, degraded alerting)
- [x] 2.2 Write tests covering the new behavior (core replication_apply.rs: 8 — convergence, byte-identity, gaps, forged edges, auto-inc promotion, AS OF parity, stream helpers, transfer errors; server replication_e2e.rs: 7 over the real wire; unit suites for epoch/frames/reader/barrier/fencing/quorum math/stale batches)
- [x] 2.3 Run tests and confirm they pass (all suites green; coverage ≥90% floor)

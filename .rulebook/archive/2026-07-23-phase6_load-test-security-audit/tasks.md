## 1. Implementation
- [x] 1.1 Load generator + sustained load test — **`fluxum-bench load`** (new `load.rs`): N pipelined connections (SDK-032) hammer `add_task` for the window; throughput is the **`fluxum_reducer_calls_total` counter delta** over the window (the TST-060-mandated method — `parse_reducer_counts` + a keep-alive-aware `/metrics` scrape), split by outcome, requiring zero errored calls. **Measured 2026-07-23: 16×32 for 60 s → 54,228 calls/s, 0 err, 0 shard-busy.** NFR-01's ≥ 100k/s is **NOT met — ceiling measured and attributed**: the shard single-writer commit loop at ~15.6 µs/commit (P0-B), invariant to connections 8→24 and window 8→128, queue never full. Not a correctness failure (every call committed). Recorded in `docs/reports/T6.6-load-and-security-audit.md`; the batch-drain lever is named in SPEC-013 TST-060
- [x] 1.2 Fan-out latency under load — **`fluxum-bench fanout`**: M subscribers on a hot channel, commit→receipt p99 from a shared instant epoch. **200 subscribers → p99 1.89 ms ✅; 1,000 subscribers → p99 8.83 ms ❌** (NFR-04 target < 5 ms). The 1,000-subscriber miss is the **same measured environmental floor** as P0-A/parity-e2e: Windows-loopback WSASend × 1,000 sockets + 1,000 driver reader threads contending for the server's cores — delivery transport + harness, not fan-out evaluation (SUB-024 sharing confirmed by the clean 200-sub run). Linux-host rerun is the named follow-up. Connection opens paced to clear the SEC-053 new-conn rate guard (a real defense, harness adapts). Fixed a fraction-vs-percent bug in the percentile call along the way (was collapsing p50/p99/max onto max)
- [x] 1.3 Security audit — **no P0 findings** (`docs/reports/T6.6-load-and-security-audit.md` §3), each required surface mapped to its enforcing test: **SQLi** against the subscription compiler (the closed SUB-010/011 grammar; `sql_injection_corpus.rs` — classic payloads, comment/encoding/UNION/stacked/exhaustion shapes all → 400, never a panic or plan); **RLS bypass** (`owner_only` enforced server-side DM-060 + reducer-side ownership re-checks; `admin_authz.rs`, demo ownership checks); **auth bypass** (AUTH-020 unauthenticated accepts only `Authenticate`; SEC-054 admin guard; `session_router.rs`/`connection_abuse.rs`/`tcp_loopback.rs`/`http_loopback.rs`); **rate-limit bypass** (RED-050 per-(identity,reducer), OPS-060 tenant quotas, SEC-047 source bucket, SEC-031 handshake throttle; `rate_limit.rs`/`tenant_quotas.rs`/`query_reducer_bounds.rs`). Residuals are P1/P2 (F-018 global auth ceiling, F-019 verify-only JWT, F-003 enforce/reject the remaining visibility rules) — tracked in the OWASP execution plan, none blocking G6
- [x] 1.4 Grafana dashboard — **`ops/grafana/fluxum-overview.json`**: 18 panels over **every P0 `fluxum_*` family** (liveness/lifecycle, reducer calls/s by outcome + duration p50/p99, writer queue depth + commits/rollbacks, reducer aborts, fan-out messages/rows + OBS-023 per-stage p99, subscriptions + drops, connections, auth outcomes, rejections/rate-limits/connguard), a `shard` template variable filtering all panels, and `DS_PROMETHEUS` datasource input. Provisioning in `ops/grafana/README.md`. **Coverage CI-guarded** by `dashboard_metrics.rs` — scrapes a live server's declared `# TYPE fluxum_*` families and fails if any lacks a panel, so no metric ships unmonitored
- [x] 1.5 Verification (DAG exit test) — load report + audit report present (`docs/reports/T6.6-load-and-security-audit.md`), **no P0 findings**; the dashboard renders all P0 metrics and its completeness is asserted against a live server. NFR-01 and the 1,000-subscriber NFR-04 are honestly-quantified environmental ceilings on the Windows box, not correctness gaps (zero errored calls; 200-sub fan-out within budget) — the accept-ceiling-vs-require-Linux-rerun call is the operator's, the same one flagged for the parity e2e ratio
- [x] 1.6 Gate G6 input — the metrics + dashboard criterion (PRD §12.1) is delivered and CI-guarded; the throughput benchmark is delivered with its ceiling attributed. Report is the G6 evidence artifact alongside the parity report

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — `docs/reports/T6.6-load-and-security-audit.md` (the consolidated report), `ops/grafana/README.md` (provisioning), module docs on `load.rs`, dashboard descriptions per panel; references the existing `docs/analysis/owasp-security/` threat model rather than duplicating it
- [x] 2.2 Write tests covering the new behavior — `load.rs` units (counter parsing across reducers/outcomes, delta math, malformed-line tolerance); `load_smoke.rs` integration (load driver measures by counter delta with zero err; fanout driver measures ordered commit→receipt percentiles) against a real release server on a short window; `dashboard_metrics.rs` (every declared P0 family has a panel); the injection corpus + auth/RLS/rate-limit suites already green
- [x] 2.3 Run tests and confirm they pass — load units (7) + `load_smoke` + `dashboard_metrics` green, injection corpus 4/4, full workspace suite + clippy clean. **Coverage: ~89.8% at archive, below the 90% floor** (the `load`/`fanout` command glue in `main.rs` [CLI residual, category 9] + sustained paths the short-window smokes can't reach); archived below-floor **by operator instruction (2026-07-23)** with the debt recorded in docs/COVERAGE.md as required-recovery. The `/metrics` scrape + counter parsing themselves ARE unit+smoke covered

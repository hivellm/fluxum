## 1. Implementation
- [x] 1.1 Build the kill -9 harness terminating the process at every commit boundary (before append, mid-append, after append/before ack, after ack) - zero acknowledged tx lost beyond the bounded async-write window (SPEC-002 acceptance 1, NFR-08) — `crates/fluxum-core/tests/crash_kill9.rs`: child-process driver + SIGKILL/TerminateProcess supervisor; boundaries a-f of TST-020 incl. rotation, checkpoint-write, and randomized flood kills; mid-append torn writes additionally swept deterministically in `crash_corruption.rs`
- [x] 1.2 CRC corruption drills: bit-flip and truncation injected at every byte offset of the final log entry AND on cold-tier page files (replay stops at first corrupt entry, torn tail quarantined; PageCorrupt recovery from retained root + replay) (STG-031, TIER-061/TIER-062) — `crates/fluxum-core/tests/crash_corruption.rs`: full byte sweeps through real `CommitLog::open` quarantine + `recover()`, head/middle/tail positions, page-extent bit-flip and torn-page drills with recovery from checkpoint root + replay
- [x] 1.3 10 GB commit-log recovery benchmark: timed restart with a recent checkpoint completes in < 30 s (NFR-06, STG-032) — `crates/fluxum-core/tests/recovery_bench.rs`: per-PR smoke + documented full-size command (`FLUXUM_RECOVERY_BENCH_BYTES=10737418240 cargo test --release -p fluxum-core --test recovery_bench -- --nocapture`); < 30 s asserted on full-size runs; validated locally: 10227.7 MiB log recovered in 6.46 s (1584 MiB/s; 16-core/127 GiB Windows x86_64) — nightly/G2 cadence per TST-024
- [x] 1.4 Deterministic simulation (DST) suite for storage/commitlog: seeded runtime, fault injection, model oracle (TST-130..TST-134) — new crate `crates/fluxum-dst`: splitmix64 seeded driver, seed-derived fault plan (lost fsync, torn write, bit flip, checkpoint corruption), model oracle with acceptance parity + crash-replay equivalence, chained determinism trace with run-twice check ("non-determinism detected for seed N at checkpoint M"), failing-seed reproduction via FLUXUM_DST_SEED; validated over 120 seeds × 250 ops + 40 × 300
- [x] 1.5 Process-level restart/persistence drills (TST-140/TST-141) — `crash_kill9.rs::restart_generations_preserve_and_extend_state`: graceful + immediate-kill restarts over a preserved data dir, readiness probing, stdout/stderr capture on failure (guard scoped to the storage stack — the server binary lands with SPEC-006)
- [x] 1.6 Tiered recovery equivalence: with cold tier populated, crash + recovery produces logical state identical to the all-hot case (rows, indexes, tx_id, auto-inc) regardless of pre-crash residency (STG-030, SPEC-002 acceptance 6); crash during eviction loses nothing — `crash_corruption.rs::recovery_is_identical_regardless_of_pre_crash_residency`: all-hot vs clean-cold vs torn-mid-eviction residency, identical fingerprints incl. auto-inc and next_tx_id
- [x] 1.7 Kill -9 at every checkpoint boundary (mid page write, after data/before manifest, after manifest/before CURRENT swap, after swap/before truncation) (SPEC-015 acceptance 4) — `crates/fluxum-core/tests/crash_checkpoint_boundaries.rs`: all four durability-boundary disk states constructed exactly, plus the randomized-timing kill in `crash_kill9.rs::kill_during_snapshot_write`; mid-page-write leg (torn extent → PageCorrupt) in `crash_corruption.rs`
- [x] 1.8 Verification (DAG exit test): zero committed-tx loss over the full matrix; DST green in CI — full matrix green locally on the committed tree (isolated worktree at main tip); DST suite wired into the standard `cargo test --workspace` CI path
- [x] 1.9 Gate G2 input: crash suite + recovery bench + 10x-dataset droplet run green — crash suite + DST + bench harness green together with T2.8's `pager_10x.rs` (the 10x-dataset suite) on the same tree; full-size bench + droplet-profile CI job are the G2 runner's nightly legs

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — architecture/docs as module rustdoc: `crash_kill9.rs` (harness architecture + TST-140/141 scope), `crash_corruption.rs`, `crash_checkpoint_boundaries.rs` (SPEC-015 boundary mapping table), `recovery_bench.rs` (measurement record + single-command runs, TST-007), and the `fluxum-dst` crate docs (scope + TST-134 phasing)
- [x] 2.2 Write tests covering the new behavior — 23 new tests across 5 suites (crash_kill9: 8, crash_corruption: 5, crash_checkpoint_boundaries: 4, recovery_bench: 1, fluxum-dst: 2 integration + 3 unit) exercising the T2.2/T2.3/T2.8 production stack
- [x] 2.3 Run tests and confirm they pass — `cargo test --workspace` green in an isolated worktree at main tip (ff70728); fmt + clippy (stable & nightly) green

## 1. Implementation
- [x] 1.1 Add an object-store client abstraction (S3-compatible: put/get/get-range/list/head) with endpoint/bucket/prefix/credentials from config; keep a local-fs backend so the trait is target-agnostic (OPS-010) — fluxum_core::backup::store: ArtifactStore trait + FsStore + S3Store (SigV4 over ureq, path-style, ListObjectsV2 with continuation, ranged GET, HEAD-as-existence); config: replication.archive.remote.{enabled,endpoint,bucket,prefix,region,access_key,secret_key(Secret, ${VAR})} validated at load
- [x] 1.2 Write archived commit-log segments with seekable-zstd framing (independently decodable blocks + frame index) so a byte range can be range-read without whole-segment download (OPS-010) — fluxum_core::backup::seekable: FLXZSEG1 blocks + MessagePack trailing index + fixed 12-byte trailer; applied to every REMOTE segment artifact (push + worker archiver). Deviation from the checklist's file pointer, deliberate: the LOCAL archive copy stays byte-identical (compact.rs contract — local PITR reuses the original per-entry CRCs and there is no ranged transport on a local filesystem); the seekable framing lives where range reads exist, on the object-store artifacts
- [x] 1.3 Extend `fluxum backup create` to upload the latest checkpoint + newly sealed segments to the object-store target, recording each artifact's content hash and object key in the backup manifest (OPS-010/OPS-011) — `fluxum backup create --remote` → backup::remote::push: hot local capture (staging) then content-addressed uploads (objects/<sha256>); RemoteManifest v1 (own file, tail-additive) records key+sha256+tx range+raw/stored lengths per artifact; `latest` moves only after everything it names is durable
- [x] 1.4 Incremental scheduled archival: upload only new/changed checkpoint pages and freshly sealed segments, driven off the checkpoint worker so writers never stall (OPS-011) — RemoteArchiver::sync wired into the worker's maintain() (own OS thread): checkpoint objects are content-addressed on disk (name == hash → HEAD probe), manifests and archived segments by stable key; failures WARN and retry next pass; pinned by the uploads-once-then-skips test
- [x] 1.5 Download-side integrity: on restore/PITR fetch, re-hash each artifact and fail with a precise report on any mismatch; a single injected bit-flip in an uploaded segment fails verify (OPS-011) — get_verified re-hashes every download against the manifest sha256; test flips one bit in the fixture's stored object and asserts the error names the exact key
- [x] 1.6 PITR range-read: replay resolves the target tx_id/timestamp to a segment + byte window and range-reads only that window from object storage (OPS-010) — remote::pitr: whole segments only up to the cut segment (manifest tx ranges), then a 12-byte trailer read + exact index read + only the blocks covering the cut window, stopping as soon as a fetched block passes the target; the local cut/lineage/guards machinery finishes (REP-070..072 semantics); RangeStats pins fetched < stored in test
- [x] 1.7 Verification: backup → object store → restore + PITR round-trip against a local S3-compatible fixture under sustained writes reproduces the exact head state and the boundary target record — fluxum-core/tests/backup_remote.rs: in-process S3 wire fixture (PUT/GET+Range/HEAD/ListV2), 5 tests (wire shape, push/restore exact + second-push-uploads-zero, bit-flip, PITR range economy + roll-forward guard, archiver incremental); fluxum-server/tests/backup_e2e.rs extended: remote push DURING sustained writes, remote restore + reboot equals the push head

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation (SPEC-025 status row; DEPLOYMENT.md offsite-targets section; config.example.yml remote block — pinned by the config_example completeness test; CLI USAGE)
- [x] 2.2 Write tests covering the new behavior (seekable unit suite; store unit suite incl. SigV4 vectors + FsStore; backup_remote.rs integration; backup_e2e remote legs; config validation via example test)
- [x] 2.3 Run tests and confirm they pass (suites green; full workspace gates before commit)

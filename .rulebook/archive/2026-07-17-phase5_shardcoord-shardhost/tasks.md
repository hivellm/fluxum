## 1. Implementation
- [x] 1.1 Implement `ShardCoord`: partition-key routing for hash, range, and geospatial-region strategies; shard registry (FR-50, FR-51); decide OQ-2 and OQ-6 — routing math lives in the new crates/fluxum-core/src/shard.rs (`PartitionStrategy::{Hash, Range, Region}` + `ShardRouter`); `ShardCoord` (crates/fluxum-server/src/shard.rs) owns the router + the `BTreeMap<ShardId, Arc<ShardContext>>` registry (duplicate ids rejected). **OQ-2 decided:** shards run as tokio tasks in one process (the spec's normative reference deployment). **OQ-6 decided:** unpartitioned tables live on shard 0, per the normative SHD-004 text
- [x] 1.2 Implement `ShardHost` per-partition loop owning its MemStore + CommitLog + SubscriptionManager (independent storage per shard) — `ShardHost { shard_id, ctx }` wraps the existing per-shard `ShardContext` (store + pipeline worker + subscriptions + fan-out), which already IS the SHD-020/021 single-writer loop; the coordinator composes N fully-independent instances (per-shard ScheduleWorkers ride the existing per-context sweeper/scheduler wiring, SHD-022)
- [x] 1.3 Routing determinism: golden vectors (SHD-003) — hash = platform-stable xxHash64 (HWA-042) over the FluxBIN key encoding, never a seeded hasher; pinned golden constants for u64/Str keys at shard_count 4; range = greatest-boundary-≤-key incl. below-first-boundary; region = `floor(coord / region_size)` verified at and around cell boundaries incl. negative coordinates (crates/fluxum-core/tests/shard_routing.rs)
- [x] 1.4 `#[fluxum::table(global)]` read-only replication (FR-53, SHD-030/031) — replicas are marked at assembly (`MemStore::set_global_replica`): reducer writes AND deletes of global tables there error with the exact SHD-031 message; after an authoritative commit, `ShardCoord::call` filters the diff's global tables and applies them to every replica's `CommittedState` via `apply_replicated_diff` (full index/unique/spatial/fulltext maintenance, NO commit-log entry, NO tx-id consumption — pinned: the replica's next tx id is still 1) **before** the call returns; replica subscribers get the fan-out via `publish_commit`
- [x] 1.5 Shard independence (SHD-020..022) — structurally guaranteed (nothing shared but the router) and pinned: a panicking reducer on shard 0 rolls back there while shard 1 serves reads and replication unaffected; queue saturation is per-shard by the existing bounded-pipeline design (TXN-011)
- [x] 1.6 Lifecycle (SHD-060/061) — per-shard kill -9 recovery rides the existing SPEC-002 replay (crash_kill9/recovery suites); the drain half of graceful shutdown is `ShardCoord::drain()`: a barrier job through each shard's FIFO single-writer queue — when it completes, every in-flight reducer has finished; the final-snapshot + log-close steps belong to the assembly that owns the checkpoint repos (documented; the binary assembly is the phase-6 CLI task)
- [x] 1.7 Verification (DAG exit test): single- and multi-shard boot tests — crates/fluxum-server/tests/shard_coord.rs (3 tests: single-shard degenerate boot; two-shard boot + sync global visibility-before-return + no-replica-log + SHD-031 gate; independence under panic + deterministic affinity + drain barrier + router placement incl. globals→authority)

## 2. Tail (docs + tests — check or waive with tailWaiver)
- [x] 2.1 Update or create documentation covering the implementation — shard.rs module rustdocs in both crates (determinism contract, decided OQs, replication semantics, drain-vs-snapshot split), SHD references per item
- [x] 2.2 Write tests covering the new behavior — shard_routing.rs (golden vectors + formula checks) + shard_coord.rs (boot/replication/independence/drain); per-table SHD-013 config strategies ride `ShardRouter::set_strategy` (the config.yml plumbing lands with the multi-shard binary assembly)
- [x] 2.3 Run tests and confirm they pass — `cargo test --workspace` green (0 failures), `cargo clippy --workspace --all-targets` clean, coverage gate ≥90%

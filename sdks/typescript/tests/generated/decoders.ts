// Code generated by `fluxum generate --lang typescript`. DO NOT EDIT.
//
// schema_version:   1   (the module's schema — SPEC-010 MIG-001)
// document_version: 1   (the /schema shape — frozen at T6.1)
//
// Re-run the generator after a schema change; edits here are lost.

import { decodeRow } from '@hivehub/fluxum';
import type { FluxType } from '@hivehub/fluxum';
import type { ChatMessage, OnlineUser, Task } from './types.ts';

/**
 * Column layout of `ChatMessage`, in FluxBIN wire order.
 */
const ChatMessageColumns = [{ name: 'id', type: 'U64' as FluxType }, { name: 'sender', type: 'Identity' as FluxType }, { name: 'channel', type: 'U32' as FluxType }, { name: 'content', type: 'Str' as FluxType }, { name: 'sent_at', type: 'Timestamp' as FluxType }] as const;

/**
 * Decode one `ChatMessage` row from its FluxBIN bytes.
 * The columns are read in declaration order; a length mismatch throws.
 */
export function decodeChatMessage(row: Uint8Array): ChatMessage {
  return decodeRow(row, ChatMessageColumns) as unknown as ChatMessage;
}

/**
 * Column layout of `OnlineUser`, in FluxBIN wire order.
 */
const OnlineUserColumns = [{ name: 'connection', type: 'ConnectionId' as FluxType }, { name: 'identity', type: 'Identity' as FluxType }, { name: 'connected_at', type: 'Timestamp' as FluxType }] as const;

/**
 * Decode one `OnlineUser` row from its FluxBIN bytes.
 * The columns are read in declaration order; a length mismatch throws.
 */
export function decodeOnlineUser(row: Uint8Array): OnlineUser {
  return decodeRow(row, OnlineUserColumns) as unknown as OnlineUser;
}

/**
 * Column layout of `Task`, in FluxBIN wire order.
 */
const TaskColumns = [{ name: 'id', type: 'U64' as FluxType }, { name: 'owner', type: 'Identity' as FluxType }, { name: 'title', type: 'Str' as FluxType }, { name: 'done', type: 'Bool' as FluxType }] as const;

/**
 * Decode one `Task` row from its FluxBIN bytes.
 * The columns are read in declaration order; a length mismatch throws.
 */
export function decodeTask(row: Uint8Array): Task {
  return decodeRow(row, TaskColumns) as unknown as Task;
}


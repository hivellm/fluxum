// Code generated by `fluxum generate --lang typescript`. DO NOT EDIT.
//
// schema_version:   1   (the module's schema — SPEC-010 MIG-001)
// document_version: 1   (the /schema shape — frozen at T6.1)
//
// Re-run the generator after a schema change; edits here are lost.

import type { ConnectionId, Identity, Timestamp } from './client.ts';

export type { ConnectionId, Identity, Timestamp };

/**
 * Row of the `ChatMessage` table.
 * Primary key: `id`.
 * `id` is auto-inc: send 0 on insert and the server assigns it.
 */
export interface ChatMessage {
  /**
   * Primary key.
   * Auto-increment.
   */
  id: bigint;
  sender: Identity;
  channel: number;
  content: string;
  sent_at: Timestamp;
}

/**
 * Primary key of `ChatMessage`, in declaration order.
 */
export type ChatMessageKey = [bigint];

/**
 * Row of the `OnlineUser` table.
 * Primary key: `connection`.
 */
export interface OnlineUser {
  /**
   * Primary key.
   */
  connection: ConnectionId;
  identity: Identity;
  connected_at: Timestamp;
}

/**
 * Primary key of `OnlineUser`, in declaration order.
 */
export type OnlineUserKey = [ConnectionId];

/**
 * Row of the `Task` table.
 * Primary key: `id`.
 * `id` is auto-inc: send 0 on insert and the server assigns it.
 * Row visibility: `owner_only` (SPEC-005).
 */
export interface Task {
  /**
   * Primary key.
   * Auto-increment.
   */
  id: bigint;
  owner: Identity;
  title: string;
  done: boolean;
}

/**
 * Primary key of `Task`, in declaration order.
 */
export type TaskKey = [bigint];

/**
 * Row of the `__idempotency__` table.
 * Primary key: `identity`, `reducer`, `key`.
 */
export interface __idempotency__ {
  /**
   * Primary key.
   */
  identity: Identity;
  /**
   * Primary key.
   */
  reducer: string;
  /**
   * Primary key.
   */
  key: string;
  created_us: bigint;
}

/**
 * Primary key of `__idempotency__`, in declaration order.
 */
export type __idempotency__Key = [Identity, string, string];


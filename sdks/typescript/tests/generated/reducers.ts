// Code generated by `fluxum generate --lang typescript`. DO NOT EDIT.
//
// schema_version:   1   (the module's schema — SPEC-010 MIG-001)
// document_version: 1   (the /schema shape — frozen at T6.1)
//
// Re-run the generator after a schema change; edits here are lost.

import type { ReducerCaller } from './client.ts';

/** The module schema version these wrappers were generated from. */
export const SCHEMA_VERSION = 1;

/**
* Typed wrappers for this module's client-callable reducers.
*
* Construct with any client satisfying `ReducerCaller`; each method
* forwards its arguments positionally, exactly as the server declares
* them.
*/
export class Reducers {
  // A real private field, not a TS parameter property: the emitted
  // code must be type-ERASABLE so Node's strip-only TypeScript mode
  // can load it directly.
  readonly #client: ReducerCaller;

  constructor(client: ReducerCaller) {
    this.#client = client;
  }

  /**
   * Call the `add_task` reducer.
   */
  add_task(title: string): Promise<void> {
    return this.#client.callReducer('add_task', [title]);
  }

  /**
   * Call the `complete_task` reducer.
   */
  complete_task(id: bigint): Promise<void> {
    return this.#client.callReducer('complete_task', [id]);
  }

  /**
   * Call the `send_chat` reducer.
   * Rate-limited to 20/s per identity (RED-050); over that the call rejects with a retryable 429.
   */
  send_chat(channel: number, content: string): Promise<void> {
    return this.#client.callReducer('send_chat', [channel, content]);
  }
}
